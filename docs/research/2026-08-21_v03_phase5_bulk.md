# Phase 5 — Bulk extraction, kernel-v03 (task 2026-08-21_v03_visibility_kernel)

Run: 2026-08-21 22:25 → 2026-08-22 01:53 UTC (after the 21:50–22:23 pilot). Profile `kernel_v03` (`scripts/run_profiles.yaml`): epoch `kernel-v03`, shard `events/batch-006.jsonl`, raw `events/raw/kernel_v03/`, model `claude-opus-4-8` pinned, prompt 0.3.0, schema 0.3, Max OAuth (`ANTHROPIC_API_KEY` unset). Log: `logs/kernel_v03_bulk.log`.

## Rate gate (AUTH-5)
- **Dry-run rate doc:** `schema-org-webapi` (first in clause→size order), 9,728 chars — **158 s wall, 111,409 tokens**.
- Projection used the pilot's five points as well (a single 10K-char doc under-predicts the long specs): fit wall ≈ 287 s + 1.89 s/Kchar → remaining 58 docs / 2,327,470 chars = **5.84 h serial**, exceeding the ≈4.4 h left in the 5 h window. Fleet of 2 (operator's hard ceiling `MAX_FLEET_WORKERS`; 2026-07-09 precedent) → 2.92 h projected. Order: inclusion-rule clause a→b→c→d then chars ascending (profile `order: clause_then_size_asc`, clause read from each `manifest_add`). **No cut point was needed: all 58 completed** (fleet wall 11,750 s = 3h16m; actual ≈ 1.12× projection).
- Window accounting: pilot 33 min + rate doc 3 min + fleet 3h16m + one retry 9 min ≈ **4h01m** of extraction wall-clock — inside AUTH-5.

## Outcome
| | |
|---|---|
| manifested kernel docs | 63 |
| extracted | **63/63** (5 pilot + 58 bulk) |
| manifested-not-extracted | 0 |
| model calls | 63 (+1 failed invocation, see below) |
| nodes / edges / quarantined (admitted events) | 3,929 / 5,405 / 103 |
| pooled quarantine rate | 0.0109 |
| empty extractions | 0 |
| tokens (envelope usage, all four classes) | **6,864,272** |
| cost | **UNKNOWN** — the control plane reports 63 unpriced calls; the per-call `cost_usd` the CLI envelope reports sums to $86.43 but is recorded as a lower-bound estimate, not the spend |

- **Failure + retry:** `wu-2025-what-generative-search-engines-like` (102,752-char PDF) failed once at `model_invocation` ("model response contains no parseable JSON object", 00:13 UTC, shard worker 1). Verbatim retry (`--only`, attempt 2/2) succeeded: 99n/141e/0q, 173,967 tokens, 554 s. Raw of both attempts persisted.
- **Quarantine flags (task: > 0.15 flagged, not re-run): none.** Two docs exceeded the runner's stricter pre-registered 0.10 and were recorded as `bulk_doc_quarantine_high` findings under `BURN_QUARANTINE_STOP_MODE=systemic` (precedent 2026-07-08; chosen because the task says flag-not-rerun and the per-doc mode would have halted the fleet): `chen-2025-geo-how-to-dominate-ai-search` 0.1245 (34q), `anthropic-crawler-support-article` 0.1158. No systemic STOP (never 3 consecutive). Top five by rate: `chen-2025-geo-how-to-dominate-ai-search` 0.124, `anthropic-crawler-support-article` 0.116, `google-search-console-start` 0.083, `schema-org-datafeed` 0.062, `census-api-user-guide` 0.055.
- **Oversize:** 0 deferred (Phase 2's extent trims held every doc under 250K; `oversize_allow` empty by design, DD-011).
- Budget: started the run with 55,000,000 tokens left in the declared band; never tripped.

Per-doc metrics: `corpus/staging/metrics/<doc_id>.json` and `corpus/staging/metrics/quality_monitors.json`; progress log appended by the runner to `docs/research/2026-08-21_v03_burn_progress.md`.
