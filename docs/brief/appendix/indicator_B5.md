<!-- generated by scripts/build_brief_pack.py (cc_tasks/2026-09-22_brief_material_pack_v2.md); framework record at commit 9ffcffddbf8f (framework/ai_readiness_framework.json, generated_from docs/crosswalk/usafacts_operationalization_skeleton.md); cycle of record scan_2026-09-10_rj4. Do not edit: re-run the generator. -->
# B5: Same concept ⇒ same identifier across products/vintages

Record node `ind:B5`. Every value below is a field of that node or of an edge or node it reaches, printed verbatim.

| field | value |
|---|---|
| criterion | B |
| construct | Semantic consistency |
| type | DOC |
| access tier (`tier`) | agency_instrumented |
| measurement tier | M |
| measurement basis | harness_leg |
| measurement status | harness_built |
| status | draft |
| candidate (DD-054) | no |
| current rule | B5: RULE-B5-v1 |
| gap | **gap** |

## Measurement spec

`spec:B5`

| field | value |
|---|---|
| collector | schema-terms |
| collector_pin | assessment/harness/scan/rules/_schema_terms.py over structured_data.fetch's A6 observation (`parsed.raw`, extruct>=0.17) |
| evidence_kind | A6's extracted markup, every product surface of the body |
| indicator_code | B5 |
| leg | B5 |
| mode | auto |
| note | Judged per body (`rules.body_groups`), on the body's well-known row. The cross-vintage half is unmeasured until a second cycle with term codes. |
| prior_art | `schema-org-definedterm` |
| rule_id | RULE-B5-v1 |
| signal | Once per body, over A6's markup of every product surface of the body on one cycle: test whether each concept (a `DefinedTerm` name) coded on two or more products carries one identifier (`termCode` within `inDefinedTermSet`). |

## Evidence

| doc_id | doc_type | title | locator (evidence cell) |
|---|---|---|---|

Evidence cell, verbatim:

> **gap**

## Actions (prescriptions)

| action | title | outcome | rule | effort | cost |
|---|---|---|---|---|---|
| act:b5-code-the-bodys-concepts-in-one-term-set | Code the body's concepts in one published term set | no_term_codes | RULE-B5-v1 | days | staff_time |
| act:b5-name-the-set-each-term-code-belongs-to | Name the term set each term code belongs to | codes_without_set | RULE-B5-v1 | hours | none |
| act:b5-use-one-identifier-per-concept | Use one identifier for each concept across the body's products | codes_not_shared_across_products | RULE-B5-v1 | days | staff_time |

## Requirements (what stands between it and a verdict)

| requires | kind | closes | route | test |
|---|---|---|---|---|
| pre:second-scan-cycle | second_cycle | unmeasured_half | second_cycle | one concept carrying one identifier across two vintages |

## Verdicts on the cycle of record (scan_2026-09-10_rj4)

| leg | matrix | pass | fail | not_applicable | error | cells |
|---|---|---|---|---|---|---|
| B5 | product | 0 | 19 | 0 | 4 | 23 |

## Other record fields

| field | value |
|---|---|
| tier_note | The rule compares the body's product pages on one cycle: a concept (a `DefinedTerm`'s name) coded on two or more products must carry one identifier (`termCode` within `inDefinedTermSet`). Judged once per body, on its well-known row. The cross-vintage half is `unmeasured_until: second cycle with term codes`. |
| tier_rule | cc_tasks/2026-09-17_measurement_tiers.md decision 2 rule 1 |
| tier_source | rules.CURRENT['B5'] = RULE-B5-v1 (assessment/harness/scan/rules/__init__.py); measurement spec `spec:B5` |
