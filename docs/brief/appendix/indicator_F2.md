<!-- generated by scripts/build_brief_pack.py (cc_tasks/2026-09-22_brief_material_pack_v2.md); framework record at commit 9ffcffddbf8f (framework/ai_readiness_framework.json, generated_from docs/crosswalk/usafacts_operationalization_skeleton.md); cycle of record scan_2026-09-10_rj4. Do not edit: re-run the generator. -->
# F2: API/schema changes are versioned; breaking changes announced with deprecation windows; compatibility checked mechanically

Record node `ind:F2`. Every value below is a field of that node or of an edge or node it reaches, printed verbatim.

| field | value |
|---|---|
| criterion | F |
| construct | Contract stability |
| type | AUTO/DOC |
| access tier (`tier`) | public |
| measurement tier | O |
| measurement basis | open_tool |
| measurement status | specified |
| status | draft |
| candidate (DD-054) | no |
| current rule | none |
| gap |  |

## Measurement spec

`spec:F2`

| field | value |
|---|---|
| collector | none_known |
| collector_pin | n/a |
| evidence_kind | two dated schema documents + diff |
| indicator_code | F2 |
| leg | F2 |
| mode | auto |
| note | Needs a version history; a single-point scan cannot observe stability. Becomes collectible once the scan runs twice. |
| prior_art | `odcs-open-data-contract-standard` |
| rule_id | RULE-F2-v0 |
| signal | Compare the API/schema description across two releases; detect breaking changes and test for a declared deprecation window. |

## Evidence

| doc_id | doc_type | title | locator (evidence cell) |
|---|---|---|---|
| odcs-open-data-contract-standard | standard | Open Data Contract Standard (ODCS) — Definition | general support; no pinpoint |

Evidence cell, verbatim:

> `odcs-open-data-contract-standard`

## Actions (prescriptions)

No `REMEDIATES` edge reaches this indicator.

## Requirements (what stands between it and a verdict)

| requires | kind | closes | route | test |
|---|---|---|---|---|
| pre:second-scan-cycle | second_cycle | tier | oasdiff_two_releases | breaking changes between two dated OpenAPI descriptions of the product's API |
| tool:oasdiff | open_source | tier | oasdiff_two_releases | breaking changes between two dated OpenAPI descriptions of the product's API |

## Verdicts on the cycle of record (scan_2026-09-10_rj4)

No leg of this indicator is a column of the cycle of record's matrices.

## Other record fields

| field | value |
|---|---|
| tier_note | No harness path runs it under this project's manners, evidence retention and re-derivation discipline, which is what keeps this O rather than M (DN-005 §2.2). Nothing was measured when the tier was assigned. It reaches all three clauses only for an API that publishes an OpenAPI description; an API that publishes none is a finding about F2, not a comparison oasdiff can make. The deprecation-window and version-bump detail is in DEPRECATION.md and VERSIONING.md, which the README links and which were not fetched. Spec `spec:F2`'s note holds: two dated releases are needed, so a single-point scan cannot run it. |
| tier_rule | cc_tasks/2026-09-18_tool_docs_ingest.md decision 2 |
| tier_source | corpus/tools/oasdiff/README.md (doc_id `oasdiff-readme`): "Command-line tool to compare and detect breaking changes in OpenAPI specs"; 'Compare two specs': "`breaking` — only the changes that break existing API clients"; 'API lifecycle': "Deprecate APIs and parameters" and "Version bumps — report a breaking change released without a major version bump"; definition of `ind:F2`: "compatibility checked mechanically" |
