<!-- generated by scripts/build_brief_pack.py (cc_tasks/2026-09-22_brief_material_pack_v2.md); framework record at commit 9ffcffddbf8f (framework/ai_readiness_framework.json, generated_from docs/crosswalk/usafacts_operationalization_skeleton.md); cycle of record scan_2026-09-10_rj4. Do not edit: re-run the generator. -->
# E5: Seeded known-bad items (canaries/decoys) in every continuous-eval cycle; a cycle with zero fired controls is INVALID, not passing

Record node `ind:E5`. Every value below is a field of that node or of an edge or node it reaches, printed verbatim.

| field | value |
|---|---|
| criterion | E |
| construct | Positive controls |
| type | AUTO |
| access tier (`tier`) | public |
| measurement tier | M |
| measurement basis | harness_leg |
| measurement status | harness_built |
| status | draft |
| candidate (DD-054) | no |
| current rule | E5: RULE-E5-v2 |
| gap |  |

## Measurement spec

`spec:E5`

| field | value |
|---|---|
| collector | control_fixtures |
| collector_pin | assessment/harness/scan/fixtures (body_two_products, body_two_products_split, fails_all, invalid_route_unobserved, passes_all, refuses_identified_client, resets_connection, resets_links_only, robots_404_html, robots_forbids_product, sitemap_on_sibling) |
| evidence_kind | per-fixture verdict map for every rule in the cycle |
| indicator_code | E5 |
| leg | E5 |
| mode | auto |
| note | Was `none_known`: a seeded canary is a property of the harness's own cycle, not an observation of an external surface. Task §4 makes the fixtures that property. |
| prior_art | internal: DD-019 decoy discipline |
| rule_id | RULE-E5-v2 |
| signal | Every control fixture is scanned before any real host. A cycle in which any of them produced an unexpected verdict is INVALID. |

## Evidence

| doc_id | doc_type | title | locator (evidence cell) |
|---|---|---|---|

Internal references: `internal:DD-019`.

Evidence cell, verbatim:

> internal: DD-019 decoy discipline; methodology §7.5

## Actions (prescriptions)

| action | title | outcome | rule | effort | cost |
|---|---|---|---|---|---|
| act:e5-fire-every-declared-control-before-the-first-host | Fire every declared control fixture before the cycle contacts a host | zero_controls_fired | RULE-E5-v2 | hours | none |
| act:e5-investigate-the-control-verdict-that-moved | Investigate a control verdict that moved, and never retune the expectation | control_verdict_not_as_expected | RULE-E5-v2 | hours | none |
| act:e5-order-the-controls-before-the-surfaces | Order the control fixtures ahead of every real host in the cycle | controls_ran_after_surfaces | RULE-E5-v2 | hours | none |

## Requirements (what stands between it and a verdict)

No `REQUIRES` edge.

## Verdicts on the cycle of record (scan_2026-09-10_rj4)

No leg of this indicator is a column of the cycle of record's matrices.

## Other record fields

| field | value |
|---|---|
| not_measured_reason | {"counts": {"error": 0, "fail": 0, "not_applicable": 0, "pass": 0}, "cycle": "scan_2026-09-09", "legs": ["E5"], "reason": "E5 judges the CYCLE, not a surface, so DD-055's definition of `measured` — a Finding on an admitted, observable surface — cannot apply to it. Its control Finding fired in this cycle and every other; the status is a limitation of the definition, not a gap in the measurement.", "recorded_by": "cc_tasks/2026-09-08_scan_run_3b.md"} |
| tier_note | The rule judges this instrument's own cycle (its control fixtures), not a publisher's; `not_measured_reason` on this node says why that is not `measured`. |
| tier_rule | cc_tasks/2026-09-17_measurement_tiers.md decision 2 rule 1 |
| tier_source | rules.CURRENT['E5'] = RULE-E5-v2 (assessment/harness/scan/rules/__init__.py); measurement spec `spec:E5` |
