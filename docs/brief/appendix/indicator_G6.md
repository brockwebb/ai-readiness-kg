<!-- generated by scripts/build_brief_pack.py (cc_tasks/2026-09-22_brief_material_pack_v2.md); framework record at commit 9ffcffddbf8f (framework/ai_readiness_framework.json, generated_from docs/crosswalk/usafacts_operationalization_skeleton.md); cycle of record scan_2026-09-10_rj4. Do not edit: re-run the generator. -->
# G6: Collection instrument/protocol carried as a versioned epoch on the series; changes annotated machine-readably with reason and inter-epoch crosswalk (SDMX break-in-series class metadata). The consumer-side test: an AI system asked to compare values across a break must surface the break

Record node `ind:G6`. Every value below is a field of that node or of an edge or node it reaches, printed verbatim.

| field | value |
|---|---|
| criterion | G |
| construct | Measurement-protocol provenance |
| type | DOC + EVAL |
| access tier (`tier`) | agency_instrumented |
| measurement tier | M |
| measurement basis | judged_reading |
| measurement status | specified |
| status | draft |
| candidate (DD-054) | no |
| current rule | none |
| gap |  |

## Measurement spec

No `MEASURED_BY` edge in the record.

## Evidence

| doc_id | doc_type | title | locator (evidence cell) |
|---|---|---|---|
| odcs-open-data-contract-standard | standard | Open Data Contract Standard (ODCS) — Definition | general support; no pinpoint |
| sdmx-3-0-section-1-framework | standard | SDMX 3.0 Technical Specifications, Section 1: Framework for SDMX Technical Standards | general support; no pinpoint |
| sdmx-standards-overview | standard | SDMX Standards (sdmx.org standards page) | general support; no pinpoint |

Evidence cell, verbatim:

> `sdmx-3-0-section-1-framework`; `sdmx-standards-overview`; `odcs-open-data-contract-standard`

## Actions (prescriptions)

No `REMEDIATES` edge reaches this indicator.

## Requirements (what stands between it and a verdict)

| requires | kind | closes | route | test |
|---|---|---|---|---|
| pre:series-break-cases | benchmark_set | tier | break_cases | whether an AI consumer comparing values across a break surfaces the break |

## Verdicts on the cycle of record (scan_2026-09-10_rj4)

No leg of this indicator is a column of the cycle of record's matrices.

## Other record fields

| field | value |
|---|---|
| tier_note | The consumer-side half is a G1-style preservation test (does the restatement keep the qualifier); the declared half, the versioned epoch carried as metadata, is a structured-field test no rule makes yet. |
| tier_rule | cc_tasks/2026-09-17_measurement_tiers.md decision 2 rule 4 |
| tier_source | docs/design/scan_tool_map.md §2 at commit 52af6c7 (verdict `content-evaluation`), confirmed against the definition; instrument: the G1 instrument: DD-036 (docs/design_decisions.md), assessment/harness/probes/g1_preservation.py, frozen at v2; definition of `ind:G6`: "The consumer-side test: an AI system asked to compare values across a break must surface the break" |
