<!-- generated by scripts/build_brief_pack.py (cc_tasks/2026-09-22_brief_material_pack_v2.md); framework record at commit 9ffcffddbf8f (framework/ai_readiness_framework.json, generated_from docs/crosswalk/usafacts_operationalization_skeleton.md); cycle of record scan_2026-09-10_rj4. Do not edit: re-run the generator. -->
# F3: Time-series identifiers, geography codes, and endpoints survive a new vintage or a crosswalk is published; tested per release

Record node `ind:F3`. Every value below is a field of that node or of an edge or node it reaches, printed verbatim.

| field | value |
|---|---|
| criterion | F |
| construct | Regression on vintage transition |
| type | AUTO |
| access tier (`tier`) | public |
| measurement tier | O |
| measurement basis | open_tool |
| measurement status | specified |
| status | draft |
| candidate (DD-054) | no |
| current rule | none |
| gap | **gap** |

## Measurement spec

`spec:F3`

| field | value |
|---|---|
| collector | none_known |
| collector_pin | n/a |
| evidence_kind | two vintages + identifier join |
| indicator_code | F3 |
| leg | F3 |
| mode | auto |
| note | Same shape as F2: requires two vintages. |
| prior_art | **gap** — no admitted source |
| rule_id | RULE-F3-v0 |
| signal | Across a vintage transition, test whether series identifiers, geography codes and endpoints survive, or a crosswalk is published. |

## Evidence

| doc_id | doc_type | title | locator (evidence cell) |
|---|---|---|---|

Evidence cell, verbatim:

> **gap**

## Actions (prescriptions)

No `REMEDIATES` edge reaches this indicator.

## Requirements (what stands between it and a verdict)

| requires | kind | closes | route | test |
|---|---|---|---|---|
| tool:wayback-cdx-server | hosted_free | tier | wayback_cdx | the endpoints captured under a product path in the prior vintage, joined to the current vintage's |

## Verdicts on the cycle of record (scan_2026-09-10_rj4)

No leg of this indicator is a column of the cycle of record's matrices.

## Other record fields

| field | value |
|---|---|
| tier_note | No harness path runs it under this project's manners, evidence retention and re-derivation discipline, which is what keeps this O rather than M (DN-005 §2.2). Nothing was measured when the tier was assigned. The index reaches the endpoints clause. Series identifiers and geography codes live in response bodies, which the index locates (timestamp, original URL, digest) and does not carry, so a rule built on this document alone would record those two clauses as unmeasured; the published-crosswalk alternative is a separate observation of the current surface. Spec `spec:F3`'s note holds: two vintages are needed. |
| tier_rule | cc_tasks/2026-09-18_tool_docs_ingest.md decision 2 |
| tier_source | corpus/tools/wayback-cdx-server/README.md (doc_id `wayback-cdx-server-api-readme`), 'Url Match Scope': "**matchType=prefix** will return results for all results under the path", with the 'Filtering' section's **from=** / **to=** date range and the 'Collapsing' section's "Only show unique urls in a prefix query" (`collapse=urlkey&matchType=prefix`) — the set of endpoints captured under a product's path in the prior vintage's date range, to join against the current vintage's; definition of `ind:F3`: "endpoints survive a new vintage" |
