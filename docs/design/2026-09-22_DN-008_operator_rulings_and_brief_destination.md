# DN-008 — Operator rulings of 2026-09-20 to 2026-09-22, and the brief as the destination

**Date:** 2026-09-22
**Status:** Active. Records rulings made in the Desktop thread that ran from the 2026-09-20 handoff to this one. Each is enacted by a completed task or a graph artifact; this note is where a stranger finds them together.
**Supersedes:** DN-007 §2's line "a hosted endpoint is a release decision" (ruling 1).

## 1. Rulings

1. **The MCP stays local. Hosting is not pursued.** Demos run from the dev machine. "Host the MCP off the laptop" is closed as an open item, not deferred.
2. **The destination is the brief** (ResearchTask `5f1bf9f0`): one chaptered deck, around 100 slides, mostly appendix, that sells the framework to the federal statistical system as first cohort with Census as first internal user. Everything else in the queue is parked debt unless it blocks the brief. Chapters: what and why; the USAFacts delta; provenance; using it; architecture; L0 results; Census dogfood; limits and roadmap; per-indicator appendix.
3. **Spend is recorded, not optimized.** Every CC task carries one clause of estimate and ends by printing its measured session total. Nothing is tuned on it now; the pairs accumulate for `bb46ddb5`. Measured pairs so far: g4_narrowed stopped run 691K (Sonnet, est. none); g4_narrowed full 3.04M (Sonnet, est. 2.5 to 4M); projection_progress, context only, not billed (Opus, est. 1.5M; measurement failed, the session could not read its own total); brief_material_pack 12.1M (Opus, est. 4M; 97 percent cache reads). The estimate is a guess until `bb46ddb5` gives it a basis; the CC RESULT is the only instrument.
4. **Checkpoints always** (`~/GitHub/CLAUDE.md` §15, 2026-09-20): a loop of paid, networked or slow calls persists per unit, resumes by skip, prints progress, pilots first, aborts cleanly on ceilings. Applied here to the projection's progress lines only (`0ac47202`): reset-and-replay cannot resume, and the note says so.
5. **G4 is narrowed** to issuing authority carried as machine-readable metadata in DCAT-US fields (`d4b1b275` and its hand run). The statutory-mandate and provenance clauses were unsourced and unmeasured; the AI-ranker sentence was the framework's own inference. Literature reviewed and recorded on the closing Issues: Wan, Wallace and Klein 2024; Schuster et al. 2026; Ge et al. 2025. Verdicts, rule, tier, version and release date unchanged.
6. **A rebuild does not re-date the release.** `build_l0_site.py` takes every published date from the record; two builds a day apart are byte-identical (`3a76dd99`). The 2026-09-20 re-dating was reverted.
7. **`seldon verify` is clean for the first time since the re-snapshot**: a `superseded_by` that resolves to a live artifact is a decision, not drift (`65e5da0e`), alongside the withdrawn exemption of DD-066.

## 2. State on 2026-09-22

- Framework frozen for the brief: 49 indicator nodes (48 in the framework, A12 candidate), 24 with a current rule, 36 M / 5 O / 5 D / 3 unassigned. Cycle of record `scan_2026-09-10_rj4` unchanged.
- Material pack: `docs/brief/`, 62 files, generated by `scripts/build_brief_pack.py`, byte-stable under `--check`, 4 Mermaid views validated, 11 demo commands executed and captured (`c3ba7b11`).
- Evidence-cell locators: 9 of 49 cells; 27 of 147 `EVIDENCED_BY` edges point at a located document; 16 indicators carry no evidence edge; 82 of 264 admitted documents are cited. Title 13, CIPSEA and the SPDs have no admitted document. The brief states these numbers rather than fixing them (`93d28c6e` holds the rest).
- census.gov on the cycle of record: 34 failing of 39 judged; hierarchical 0.080, rank 5 of 13, resting on one A10 pass; flat 0.190.
- Dispatcher: on, `stuck_after_passes: 3`, MCP writes self-commit. Queue empty.

## 3. Open, none blocking the brief

- Record lag, found by the pack and not fixed: B1, B2, B4, B5, D2, D3 and G4 read `measurement_status: harness_built` while each is judged on the cycle of record (`e56cd2fb`). The generation-0 Finding in the A10 chain has no `cycle` property.
- Seldon: a proposed task that fails header parsing is reported as `0 candidate(s)` with no reason; the After parser is not line-anchored (`fb3b11bc`). This cost seven hours and two re-issues on 2026-09-22.
- The scan layer of the projection (1150 s) still runs without in-loop progress; `publish.py` was outside `0ac47202`'s write set.
- Spend measurement: a headless session cannot read its own billed total; the pack session summed its transcript instead. `bb46ddb5` decides the instrument.
- Weighting, NSDS (`8a9a89d7`), the ER debt chain, and the rest of the 30 open tasks: parked.
