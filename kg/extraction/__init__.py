"""Extraction module (schema_v0.1.md §5 whole-document protocol, §7 state machine).

Module build only — the pilot run on documents is a separate, operator-gated task.
No LLM extraction happens here; ``model_stub`` deliberately refuses to make calls.
"""
