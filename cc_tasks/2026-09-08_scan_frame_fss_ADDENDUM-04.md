# ADDENDUM-04 to `cc_tasks/2026-09-08_scan_frame_fss.md` — the department "primary domain" is unresolvable from named sources; that is the finding. Use the derivable hosts and finish.

**Date:** 2026-09-08
**Authored by:** Desktop session, on `state/fss_department_domains_2026-09.json` (16:32Z): CISA's registry is one row per domain with no primary-domain field (3 of 26 departments resolve to a single candidate); catalog.data.gov's documented CKAN action endpoints answer 404 (0 of 3). CC stopped rather than invent a domain. Correct. But the frame task has now stopped three times on §2.a while §3 to §7 wait, and each stop was on a derivation of the *machine entry point*, which tier 0 does not need. Close §2.a as follows and do not open it again in this task.

**Decisions:**
1. **Department → primary domain is not derivable from any named source; record that as a result, not a blocker.** Register `fss_departments_primary_domain_derivable_2026-09` = 3 (of 26) with the registry digest, and `fss_catalog_data_gov_api_endpoints_available_2026-09` = 0 (already registered) stands as a Tier C tier-0 observation to be re-measured by the A11 leg in cycle 3. Append one sentence to the frame DD (§5): the government publishes no machine-readable map from a department to its Enterprise Data Inventory, and this instrument does not invent one.
2. **Inventory lookup uses the two hosts derivable from the roster and nothing else:** the agency's own host (`www.bls.gov`) and its registrable domain from the registry's reverse attribution (`usda.gov`, `ed.gov`, `cdc.gov`, `ojp.gov`, `irs.gov`, `nsf.gov`, `ssa.gov`, `samhsa.gov`, `federalreserve.gov`), `/data.json` on each. `fss_agencies_with_inventory_at_derivable_host_2026-09` = 5 already records the outcome; add the per-agency status to the targets file (`inventory: found_at_agency_host | found_at_registrable_domain | inventory_present_agency_unattributed | none_at_derivable_host`) and register the four counts. Where an inventory is found, §2.a's API-distribution rule applies; where not, the machine entry point is `not_derivable_from_named_sources` and the surface list for that agency is home, `.well-known` probes, and declared flagships. That is a complete, honest row.
3. **No further addenda on §2.** Whatever §2 yields after this is the frame for cycle 3; cycle 3's RESULT says what the frame missed. If anything else in §3 to §7 blocks, **write the RESULT anyway**, opening with the block, the gate output for whatever ran, and the registered counts; leave the task `proposed`; commit and push. A task that stops must leave its RESULT; three stops without one is the defect this addendum is really about.
4. Commit and push at the end regardless of outcome (repo doctrine as of 2026-09-08: commit, merge, push, tag, deploy are always the task's job).

**SEQUENCING:** 1 → 2 → register targets → §3 → §4 → §5 → §6 (hard stop) → §7 → push. `cc complete` only if §6 passed.
