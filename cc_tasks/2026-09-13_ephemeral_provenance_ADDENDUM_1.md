# ADDENDUM 1 to `2026-09-13_ephemeral_provenance.md` — licences, on the operator's declaration

**Date:** 2026-09-13. **Amends** the base task; does not supersede it. Operator's own value input (2026-09-13): code under MIT; data, report and generated artefacts under CC BY 4.0 International (the Zenodo default). Under DN-002 decision 5.

**Added decisions:**
A1. **`LICENSE` at the repository root: MIT**, copyright holder as `publication.yaml` names the authors, year 2026. Governs code: `assessment/`, `kg/`, `scripts/`, `tests/`, `figures.py` and the rest of the Python.
A2. **`LICENSE-DATA` at the root: CC BY 4.0 International**, verbatim text from creativecommons.org held in the corpus if admitted, otherwise the canonical text with its URL. Governs `docs/reports/`, `docs/data/`, the matrices, `framework/ai_readiness_framework.json`, the registered Results and the sources appendix.
A3. **The corpus is excluded in writing.** `corpus/` documents are third-party works held under their own terms; `LICENSE-DATA` says so in one sentence, and the corpus manifest's published copy carries the same sentence in its `index.json` note.
A4. **`publication.yaml` declares both** (`license_code: MIT`, `license_data: CC-BY-4.0`); `CITATION.cff` carries `license: CC-BY-4.0` with the code licence in `notes` (CFF's `license` is for the cited artefact, which is the report); `.zenodo.json` carries `"license": "CC-BY-4.0"`. `docs/index.html` states both licences in its fixed prose, generated from the declaration.
A5. **A test** asserts both files exist, both SPDX identifiers appear in the declaration and in the three consumers, and the corpus exclusion sentence is present.

**Gate (§3) gains:** A5 green; `docs/index.html` and `data/` regenerated by the site builder, no other `docs/` change. The zero-edits line's "`docs/` beyond `data/results_tagged.json`" is widened to `docs/index.html`, `docs/data/index.json` and the two citation copies, for this addendum only.

**Sequencing unchanged:** the addendum's work runs inside §2 of the base task.
