# CC Task — A1, A8, B3, D4 get their sources, or the report keeps saying they have none

**Date:** 2026-09-12
**Project:** ai-readiness-kg
**Authored by:** Desktop session, OODA on `2026-09-11_report_sources_appendix_RESULT.md` §5, §6 item 2, §8.
**Implements:** DN-001 (`docs/design/2026-09-12_DN-001_report_citation_invariant.md`), the report citation invariant, decisions 1, 3, 4, 5; under DD-001 and DD-003.
**Fulfils:** its own ResearchTask (`seldon cc register`). Runs before publish. After this task the report publishes whatever the appendix says; a further "no admitted source" row is a true statement, not a blocker.
**Spend:** zero model calls. **Network: none by default.** DD-003's search order is corpus before web, and the previous sources task found every document already admitted. Acquisition is permitted for exactly one document under decision 4 and nothing else; the socket counter asserts it.

**Why this task exists.** Four product checks the report quotes a pass rate for, A1 (structured data), A8 (current-vintage pointer), B3 (content without script execution), D4 (public inventory entry), reach no source document in the graph and the appendix now prints that in the PDF. It is the defect the A3/A10 task closed, one column over, found because the appendix was told to cover every check the prose names rather than the seven the traceability test pins.

**Prior art, so the sources are searched for and not invented.** The corpus is read first; the candidates below are what a reader of the corpus manifest would expect to find, and CC verifies each locator from the document, rejecting any whose title fits and content does not (the BP 10 lesson).
- **A1** structured data: `schema-org-dataset` (already admitted); W3C DWBP on machine-readable, standardised formats and on providing metadata (`w3c-dwbp-2017`); DCAT-US as the federal metadata vocabulary (`dcat-us-1-1-schema`).
- **A8** current-vintage pointer: DWBP's version-indicator and version-history practices; DCAT-US `modified` and `accrualPeriodicity`; FAIR R1.2 provenance.
- **B3** content without script execution: this may have no standards-grade source. The admitted crawler documents (`openai-crawlers-bots`, `anthropic-crawler-support-article`, `perplexity-crawlers`, `cloudflare-ai-crawl-control-manage-crawlers`) are read for any statement that the crawler does not execute scripts; DWBP is read for any practice on content availability without client-side execution. If nothing admitted states it, B3's row stays "no admitted source" and the RESULT says what was read and what it did not say.
- **D4** public inventory entry: DCAT-US as the Public Data Listing schema (`dcat-us-1-1-schema`); OMB M-13-13 is the mandate itself.

**Decisions taken here (operator overrides later):**
1. **Corpus first, edges only.** Every citation that an admitted document supports is an evidence-cell entry in the skeleton, carried by `framework_writeback_evidence.py` through the single writer (which now refuses drops and logs the delta), then projected. No node is created by hand.
2. **`report_traceability.LEGS` and its test widen to the checks the prose names**, read from the sections as `appendix_legs()` already does, so the seven-leg pin becomes a twelve-leg pin with the same shape assertions (≥ 1 construct, spec, rule) and a source floor of ≥ 1 for every leg that this task cites, and an explicit, named allowance of 0 for any leg it could not.
3. **The report is rebuilt** through `make report-pdf` so the appendix reflects the graph; no section prose changes; F5 unchanged; prose and total page counts re-registered under `_2026-09-12` names only if they moved.
4. **One acquisition is permitted: OMB M-13-13, for D4 only**, and only if `dcat-us-1-1-schema` on reading does not itself state the Public Data Listing requirement. It enters through `kg/manifest.py add`, the Dixie sweep, `rebuild`, and the projection, with `acquisition.acquired_at` set, the field the corpus currently lacks everywhere. If DCAT-US states the requirement, the memorandum is not acquired and the RESULT says so.
5. **Missing `pub_year` on seven admitted documents and a missing author on one** (previous RESULT §3) are not this task's. They are queued; the appendix renders what the manifest holds.

**Zero edits to:** rule modules, harness, payloads, prior Results, prior RESULTs, figures, existing Document nodes, section prose, the record beyond the four evidence cells (and A10's cell untouched).

**Immutable once written. Glob `2026-09-12_a1_a8_b3_d4_sources_ADDENDUM*.md` before starting and again before §3.**

---

## 1. Decisions 1, 4. Read, cite from the corpus, decide the one acquisition. Stop if any write-back is refused by the single writer; a refusal here means the cell edit would drop something, and that is a finding before it is a fix.
## 2. Decisions 2, 3. Widen the pin; rebuild; page counts.
## 3. Gate (the one gate of this task)
Traceability test green over twelve legs with the stated floors; every appendix row's doc_id in the manifest; A1, A8 and D4 ≥ 1 source each (B3 ≥ 1 or a named allowance with the reading recorded); write-back events carry deltas with 0 removals; projection round-trip green; tag-coverage lint and PDF numeral-multiset gate green; `test_report_text_and_figures_agree_per_leg` green; embedded F5 unchanged; socket counter shows at most the one acquisition host; `make gate-task`; `make gate-full` detached, logged, polled; `seldon verify`; protected paths.
**Failure writes no edges and no PDF: report and stop, RESULT with the block on top, commit, push.**
## 4. Report
RESULT `cc_tasks/2026-09-12_a1_a8_b3_d4_sources_RESULT.md`: per leg, the sources and locators verified from the documents, and for B3 what was read and what it said; whether M-13-13 was acquired and why; the appendix rows after; page counts; every premise wrong. `seldon cc complete`, commit, push. Final message states the twelve-leg source counts, the PDF path, and whether the push succeeded.

**SEQUENCING:** §1 (stop on a refused write) → §2 → glob addenda → §3 (detached, logged, polled) → §4 → push.
