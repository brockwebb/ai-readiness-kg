# CC Task — re-judge cycles 2, 3 and 4 under harness-v5; register; rebuild the L0 draft from cycle 3 re-judged

**Date:** 2026-09-10
**Project:** ai-readiness-kg
**Authored by:** Desktop session, OODA on `2026-09-10_scan_run_4_RESULT.md` §7 items 2 and 3.
**Fulfils:** its own ResearchTask (`seldon cc register`). Runs after `2026-09-10_harness_v5_blind.md` (its RESULT must show gate PASS). Precedes `2026-09-10_corpus_noaa_esip.md`.
**Spend:** zero model calls. **Network: none.** Everything here is re-judgement of stored Observations. No host is contacted; a test asserts the socket counter is zero for the whole task.

**Decisions taken here (operator overrides later):**
1. **Three re-judgement payloads**, each from its cycle's stored Observations under `CURRENT` (harness-v5, current rule versions): `scan_2026-09-07b_rj2` (cycle 2 already has `_rj1`), `scan_2026-09-09_rj1`, `scan_2026-09-10_rj1`. Cycle 1 is not re-judged: its registered Results carried zero verdicts on unobserved evidence (scan-run-4 RESULT §1 table starts at cycle 2); the RESULT confirms that count from the invariant test before leaving cycle 1 alone.
2. **Register from the re-judged payloads**, new names with the `_rj` suffix, for every Result the defect could have touched: leg rates and upper-95 bounds for A1, A3, A10 and any other leg whose Findings changed; `scan_l0_product_legs_at_zero_*`; the three matrices; verdict counts. Results whose value did not change are NOT re-registered; the RESULT lists them as unchanged with the check that proved it. Every old Result that a new one replaces gets a `SUPERSEDED_BY` relation to the new one if the schema has it, otherwise the new Result's description names the old one and the old one's description is not touched.
3. **Cycle 4 is registered for the first time here**, from `scan_2026-09-10_rj1`, everything scan-run-4 §4 would have registered, including A12 per tier and pooled and request count per site (2,684 over 19 sites and 35 netlocs; the site count is the manners claim).
4. **The L0 report is rebuilt from `scan_2026-09-09_rj1`.** Every `{{result:...}}` tag that pointed at a superseded cycle-3 name is re-pointed at its `_rj1` replacement; tags whose Result was unchanged stay. One new sentence in the movement section: cycle 3 was re-judged under harness-v5 after a blind-probe defect was found in cycle 4; the numbers quoted are the re-judged ones; the originals remain registered under their first names. The PDF is rebuilt through `make report-pdf`. No other prose changes.
5. **Graph page and figures refresh** from the graph, now including cycle 4.

**Zero edits to:** rule modules, harness, prior payloads, old Results, prior RESULTs, cycle evidence, targets, report prose beyond decision 4.

**Immutable once written. Glob `2026-09-10_rejudge_2_3_4_ADDENDUM*.md` before starting and again before §3.**

---

## 1. Decision 1. For each `_rj` payload, a diff against its predecessor: which Findings moved, from what to what, and which class moved them. Expected: every move is a `pass`/`fail` → `error` on a robots-disallowed probe; any other move is a stop.
## 2. Decisions 2, 3, 4, 5.
## 3. Gate (the one gate of this task)
Invariant test: 0 verdicts on unobserved evidence in all three `_rj` payloads; byte-identical re-derivation of all twelve payloads under their own harness versions; socket counter 0 for the task; report build resolves every tag with 0 unresolved; PDF numeral-multiset gate; the report's movement figure re-derives; fast tier, full suite, `seldon verify`, protected paths.
**Failure writes no Results and no report: report and stop, RESULT with the block on top, commit, push.**
## 4. Report
RESULT `cc_tasks/2026-09-10_rejudge_2_3_4_RESULT.md`: the three diffs as tables; every Result registered and every one left unchanged; what the L0 draft now says differently from what it said, number by number; the four newly live flagships and the three refused, leg by leg, from cycle 4 re-judged; every premise wrong. `seldon cc complete`, commit, push. Final message states the PDF path and whether the push succeeded.

**SEQUENCING:** §1 (stop on an unexpected move) → §2 → glob addenda → §3 (detached, logged, polled) → §4 → push.
