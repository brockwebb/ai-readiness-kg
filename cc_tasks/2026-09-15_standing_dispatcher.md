# CC Task — the standing dispatcher: a registered task with a clean gate no longer waits for a person

**Date:** 2026-09-15
**Project:** ai-readiness-kg (code lands in the Seldon repo, `/Users/brock/GitHub/seldon`; the launchd job and configuration land here)
**Authored by:** Desktop session, from `docs/design/2026-09-15_DN-006_standing_dispatcher.md`, DN-005 §4 item 1.
**Implements:** DN-006 decisions 1 to 7, 9 and 10. Decision 8 (cadence) is the next task, not this one.
**Framework layer served (DN-005 §5 rule 1):** §2.2 Tier M. The measured tier's cadence cannot be standing while its cycles wait on a pasted line.
**Fulfils:** its own ResearchTask (`seldon cc register`), and supersedes `6ee71737` (the design half of that task is DN-006; this is the build half).
**Spend:** zero model calls. The stub CC used by the tests is a shell script, not the CLI. **Network:** none.

**Decisions taken here (operator overrides later):**
1. **Home.** `seldon dispatch once|status|lease reap` in the Seldon repo, reading a `dispatch:` block from the project's `seldon.yaml`. Read `docs/design/2026-09-15_DN-006_standing_dispatcher.md` in full before writing a line; its §3 decisions are the spec and its §5 table is the parameter block. Where this task and the note differ, the note wins and the RESULT names the difference.
2. **Eligibility is DN-006 §3 decision 2, all eight criteria, each evaluated to a recorded value.** The candidate set is tasks whose file carries both `**Spend:**` and `**Network:**` headers; the parser reads the same header lines `seldon cc register` already parses and adds nothing to the file format. c4 resolves the standing band through a reference (`seldon.yaml dispatch.standing_band_ref: controls.yaml#spend.daily_tokens`), never a copied number; a test asserts the resolved value equals what `kg/spend.py` reads.
3. **The supersession marker (DN-006 decision 3).** Step 0 greps every existing `cc_tasks/*_ADDENDUM*.md` in this repo for how supersession was expressed in practice. If a consistent marker exists, adopt it, cite the files, and write `docs/design/2026-09-15_DN-006_standing_dispatcher_ADDENDUM_01.md` correcting decision 3. If none exists, `**Status:** SUPERSEDED` within the first ten lines is the convention, and `CLAUDE.md`'s "CC dispatch protocol" section gains one sentence saying so, plus one sentence for DN-006 decision 10 (while `dispatch.enabled` is true, no hand-dispatch).
4. **Claim, launch, finish, no retry (decisions 4, 5, 7).** Claim is the `in_progress` transition with `claimed_by = dispatcher:<host>:<pid>`; a failed transition launches nothing. Launch is `claude -p "<the CLAUDE.md dispatch line for that file>"` from the project root, detached, `logs/dispatch/<stem>.log`, `EXIT=$?` appended. The non-interactive permission mode is derived from how this repo already runs `claude -p` unattended (`kg/extraction/model_stub.py`, `scripts/jobs/airkg_extraction_burn.sh`); the flag and the reason go in `seldon.yaml` with a comment, and the RESULT quotes the exact invocation. `ANTHROPIC_API_KEY` present in the environment is a `dispatch_refused` with reason `api_key_present`, before any claim. On finish: exit code, wall clock, RESULT present, graph state observed, on the `dispatch_finished` event; non-zero exit or no RESULT or no `completed` in the graph moves the task to `blocked` with the log path. Zero retries, asserted by a test that runs a failing stub twice and sees one launch.
5. **Lease (decision 6).** `fcntl.flock` on `<project>/.seldon/dispatch.lock`, JSON body with holder, `acquired_at`, `heartbeat_at`, task in flight. A second instance exits 0 with `dispatch_refused` reason `lease_held`. `lease reap` refuses while the holder PID is alive; there is no age-based reap. Two-process contention is tested with a real second process, not a mocked flock.
6. **Events go on Seldon's own event log** with the names in DN-006 decision 7 and the criteria vector as a mapping of criterion id to observed value. A pass with nothing eligible writes no event; a test asserts the log is byte-identical before and after such a pass.
7. **launchd.** A plist under this repo's `scripts/jobs/` runs `seldon dispatch once` for this project every 300 s under the anaconda interpreter the existing jobs use, with the same log caps `controls.yaml jobs.biblio_resume` declares (`log_max_line_chars`, `log_max_run_bytes`, `log_retention_days`), by reference to those keys. `seldon.yaml` ships with `dispatch.enabled: false`. **This task does not enable it.** Enabling is the first line of the cadence task, after the operator-dispatched queue has drained under decision 10.
8. **Cross-repo shape.** Seldon-side code, tests and a `docs/design/` entry in the Seldon repo describing the command group go on a branch, are merged to main, pushed and the package reinstalled here; both commits are cited in the RESULT. A task that ends on an unmerged branch is not complete.
9. **Priority is FIFO by `created_at` (decision 9).** No other ordering, and no ordering field is added.

**Zero edits to:** rule modules, manners, stored payloads, the log's existing shards, Results, prior RESULTs, figures, the report, the skeleton, the record, `corpus/`, `controls.yaml` (read only, by reference). `CLAUDE.md` changes are the two sentences in decision 3 and nothing else.

**Immutable once written. Glob `2026-09-15_standing_dispatcher_ADDENDUM*.md` before starting and again before §5.**

---

## 0. Step 0: the marker survey (decision 3). Output is a table in the RESULT: addendum file, the line that expressed supersession if any.
## 1. Seldon repo: `dispatch` command group, eligibility evaluator, lease, launch and finish paths, events. Tests: eligibility over fixture task files (one per criterion, each failing exactly one); stub `claude` on PATH that writes a RESULT and runs `seldon cc complete`; a failing stub for the blocked path and the zero-retry assertion; real two-process lease contention; STOP file and `enabled: false`; dirty tree; `api_key_present`; no-event pass. Seldon's own suite green, merged, pushed, reinstalled.
## 2. This repo: `seldon.yaml dispatch:` block (`enabled: false`, `standing_band_ref`, `branch`, `poll_interval_s: 300` with the DN-006 §5 comment that it has no measured basis, `permission_mode` with reason), the plist, `logs/dispatch/` gitignored, `.seldon/dispatch.lock` gitignored, `CLAUDE.md` sentences from decision 3.
## 3. Graph: supersede `6ee71737` with the reason "design in DN-006; build in cc_tasks/2026-09-15_standing_dispatcher.md" and this task's own artifact id as the superseding artifact. Cypher before and after in the RESULT.
## 4. Dry pass: with `enabled: false`, run `seldon dispatch once` against this project and `seldon dispatch status`; the RESULT quotes the status output, which must show every open task as **not a candidate** (no headers) except this task's own file and `2026-09-15_claude_md_cites_dn005.md`, both shown ineligible on c8. Zero events written; assert by byte comparison.
## 5. Gate (the one gate of this task)
Seldon suite green on the merged commit; here `make gate-fast` detached, logged, polled (no payload is touched; say so), `seldon verify`, protected paths; §4's byte comparison; the two `CLAUDE.md` sentences present. **Failure ships nothing on either repo: report and stop, RESULT with the block on top, commit, push.**
## 6. Report
RESULT `cc_tasks/2026-09-15_standing_dispatcher_RESULT.md`: both commits; the exact `claude -p` invocation; the marker survey table; the status output from §4; the log paths; every premise wrong, including any DN-006 decision the build showed to be wrong (write the DN-006 addendum, do not silently deviate). `seldon cc complete`, commit, push.

**SEQUENCING:** §0 → §1 (Seldon repo, merged and reinstalled before §2) → §2 → §3 → §4 → glob addenda → §5 (detached, logged, polled) → §6 → push both repos. Runs after `cc_tasks/2026-09-15_claude_md_cites_dn005.md` has pushed; both edit `CLAUDE.md`.
