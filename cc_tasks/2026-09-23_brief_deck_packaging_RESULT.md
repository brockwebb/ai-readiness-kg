# RESULT: the brief deck packaged as brief plus appendix

**Task:** `cc_tasks/2026-09-23_brief_deck_packaging.md` (no addenda). **Status:** complete. Every gate in §5 ran to its EXIT line. Commits: `ed2b3c2` (decision 3 pack change, gated alone by `check_protected_brief_pack.sh`), `05c3ebe` (the deck), and this RESULT.

## 1. Slide counts (decisions 1 and 2)
| output | cover | A | B | C | D | E | F | G | H | close | Appendix | total |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `docs/deck/brief_deck.pptx` | 1 | 3 | **13** | 5 | 16 | 5 | 3 | 11 | 9 | 1 | — | **67** |
| `docs/deck/brief_appendix.pptx` | 1 (new) | | | | | | | | | | 218 | **219** |

The old single file had 293 slides. Chapter B went from 22 slides to 13: the eleven §8 quotations are now one section, which the 14pt floor splits into two slides. The other 12 B slides were unchanged. So 293 − 9 + 2 = 286 = 67 + 219. The two added slides are the brief's closing slide and the appendix cover. The closing slide's `@stamp appendix` prints `docs/deck/brief_appendix.pptx: 219 slides`, from a real plan of the appendix. `tests/test_brief_deck.py` asserts that the two files together hold exactly as many slides as one file of the same sections, that the split adds only the cover and the closing slide, and that the stamped count equals the appendix's real count. It also asserts that every part of the §8 section except the last is full at the floor.

## 2. Diagram outcome (decision 3)
(c) and (d) are drawn `flowchart TB` (they were `LR` and `RL`). They now measure 1089×654 and 402×1022 px, where they had been 2179×222 and 2384×161. No other line of `build_brief_pack.py` changed. The only pack file that moved is `E_architecture.md`, and `numbers.json` did not change. Neither diagram exceeds the slide, so neither was split. The "how to read it" text is unchanged.

Drawing (d) upright was not enough on its own. In the stacked box (3.7 in tall), a tall diagram draws smaller than it did left to right. So the renderer now picks between two placements: *stacked* (image above the text) and *side* (image on the left at 5.55 in tall, text in a 5 in column to its right). It takes whichever draws the image larger, as long as the text fits. Below is the effective node-label size on the slide, using Mermaid's 16px default:

(a) 5.96 → 5.96 pt (stacked, unchanged); (b) 4.69 → 5.73 pt (side, by the same rule; nothing else about (b) changed); (c) 6.52 → 7.44 pt (side); (d) 5.96 → 6.26 pt (side).

(d) gains only 5 percent. The limit now is its chain of five generations stacked vertically, not the flowchart direction. Splitting it at a `SUPERSEDES` edge would roughly double the label size, but that needs a second Mermaid block in the pack and was outside this write set. (a) is also wide, at about 7:1, and this task left it alone.

## 3. 61 versus 62 (decision 4)
There are 62 files under `docs/brief/`. `build_brief_pack.py --check` renders and compares 61 of them. The 62nd is `D_demo_capture.json`: only `--capture-demo` writes it, and `--check` leaves it out of the generated set on purpose (`stray = on_disk - set(files) - {CAPTURE.name}`).

Both counts are right, they just count different things: 61 generated files, 62 files in the pack. No slide carries either count, slide 2 included. If a slide ever needs one, it should say "62 files, 61 of them regenerated by `--check`". I left DN-008 §2 alone.

## 4. Premises the task file got wrong
1. The task says the pack v2 RESULT and `docs/brief/INDEX.md` give 62. The pack RESULT says 61 twice (lines 9 and 13), INDEX states no file count, and DN-008 §2 line 20 is the only place with 62. The task also says slide 2 carries a pack count; it carries none.
2. The task assumed drawing (c) and (d) upright would enlarge them. On the existing stacked box it would not: (c) would stay at 6.52 pt and (d) would shrink to 4.2 pt. The side placement in §2 is what made them larger. B measured 13 slides, inside the expected 12 to 16.

## 5. Gate (logs under `logs/`, not shipped)
- `make gate-fast`: **2788 passed, 3 skipped, 27 deselected, 12 xfailed**, 666.87 s, EXIT=0 (`logs/pkg_gate_fast.log`). This is the fast tier, not the full suite.
- `seldon verify`: all checks passed, EXIT=0 (`logs/pkg_seldon_verify.log`).
- `check_protected_brief_pack.sh`: 61 files checked, 0 drifted, PROTECTED PATHS OK, EXIT=0. It passed when decision 3 was committed alone (`logs/pkg_d3_protected_pack.log`) and again at the final gate (`logs/pkg_protected_pack.log`).
- `check_protected_brief_deck.sh`, now diffing against the launch commit `5ed4eac`: both outputs identical, PROTECTED PATHS OK, EXIT=0 (`logs/pkg_protected_deck.log`). Its new rules: under `docs/brief/` and `docs/deck/diagrams/`, only the decision-3 files may move, and `build_brief_pack.py` may differ from the base only in the two flowchart directions and their comments.
- `tests/test_brief_deck.py`: 14 passed, 0 skipped, run inside the gate.

## 6. Spend
Measured session total up to this RESULT: **4,484,357 tokens**, of which 96.3 percent were cache reads (41 model turns), on `claude-opus-5-5`. The estimate was 2 to 4M. There were no model calls and no projection.
