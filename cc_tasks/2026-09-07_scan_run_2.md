# CC Task — scan-run-2: the second cycle, under the instrument the first one taught

**Date:** 2026-09-07
**Project:** ai-readiness-kg
**Authored by:** Desktop session (OODA on `2026-09-07_scan_hygiene_RESULT.md` and `2026-09-07_scan_harness_v3_RESULT.md`; every count below re-read from the graph 2026-09-07 evening)
**Fulfils:** its own ResearchTask (registered by `seldon cc register`); the cycle that precedes `report-draft` (`2517cda8`).
**Spend:** zero model calls. Network: the same 14 public statistical hosts as cycle 1, identified UA `ai-readiness-kg-scanner/0.1`, 1 req/s per host, robots obeyed, one worker, no forms, no logins. With the shared link probe the cycle should issue roughly 559 fewer requests than cycle 1 (harness-v3 §7.3); report the actual request count per host.

**Premise (verified):** both harness tasks completed and green; `finding_identity: 2`; `CURRENT` is harness-v3 §8 (A1-v3 and A3-v4 consume `link_probe`; 35 modules in `REGISTRY`); four control fixtures with a pre-registered verdict table in `params.yaml`; `ERROR_CLASSES` closed with `refused`, `connection_reset`, `unknown`; graph error classes now `http_4xx` 1458 / `connection_reset` 92 / `http_5xx` 44 / `robots_disallowed` 15 / `collector_unavailable` 3 / `unknown` 1, `dns` 0; 120 orphan control Findings annotated; 260 fixture blobs quarantined; `MeasurementSpec` A1/A3 point at the new rules; framework 16 / 1 / 32 unchanged; `cycle.name` still `scan_2026-09-07` so `refuse_clobber` will refuse until it moves; `run.py` outside pytest still writes into the committed evidence store; 266 observations carry `http_4xx` on a 403 that the new map would call `refused`; 418 tracked non-fixture evidence bodies are cited by no Observation; 22 `conversion_gap` events on scan surfaces remain on the log with their tasks withdrawn.

**Decisions taken here on the grounding (not escalated; operator overrides later if wrong):**
- **One error convention on the log.** `error_class` classifies what the collector saw; a 403 to the identified client on a robots-permitted path is `refused` under the closed set the harness now declares. Two conventions make `error_class_counts` incomparable across cycles, which is the one thing that metric is for. The 266 are overlaid (`observation_error_reclassified`, `error_class_recorded` retained), never edited. Rule verdicts read status, not class, so no Finding moves; the gate proves it.
- **Evidence enters the committed store only when a published payload cites it.** Staging root per run, promotion on publish. The 418 tracked uncited real-host bodies are retained where they are as the record that those hosts were contacted (manners is auditable), registered as a count, excluded from every integrity check, and never cited by anything. Deleting bytes that record contact with a public host is the wrong side of invariant 2.
- **The 22 stale `conversion_gap` events are annotated, not closed.** `conversion_gap_withdrawn` overlay citing the withdrawn task ids, the same shape as `finding_evidence_unretained`.

**Zero edits to:** any shipped rule module, the target list, the G1 harness, `assessment/cq/*.yaml`, any event line, indicator content in the JSON except `measurement_status` and `measured_by` through `framework_writeback_measured.py`, any prior cc_task or RESULT. `params.yaml` may change only in `cycle.name` and the additions §1 names.

**Immutable once written. Changes require a new task file or an `_ADDENDUM-NN.md` sibling. Glob and read all siblings `2026-09-07_scan_run_2_ADDENDUM*.md` before starting.**

---

## 1. Pre-flight (no federal request until §2)
1. `run.py --evidence-root` (default `state/evidence_staging/<cycle.name>/`); `publish.py` promotes into `corpus/evidence/scan/` only bodies whose digest an Observation in the published payload cites, and removes the staging directory. Test with a fixture collection: nothing lands in the committed store before publish; after publish, exactly the cited digests are there. `tests/conftest.py`'s guard stays.
2. Overlay the 266 `http_4xx`-on-403 observations to `refused` via `scripts/reclassify_observation_errors.py` (extend its map; new shard; idempotent). Project. Cypher: `error_class = 'refused'` count = 266 + whatever the fixtures contributed; `http_4xx` on status 403 = 0.
3. Overlay the 22 `conversion_gap` events: `conversion_gap_withdrawn` on a new shard, each naming its withdrawn ResearchTask id and `cc_tasks/2026-09-07_framework_projection_repair.md`. Project as a flag on whatever the gap event projects to; if it projects to nothing, say so and stop that step.
4. Register `scan_evidence_uncited_real_host_bodies` = the recount (expected 418; register what is counted) and append a DD recording the retention decision above (next free number; read the file).
5. `cycle.name: scan_<UTC date at this step>`; `cycle.targets: scan_targets_2026-09` unchanged; every other key unchanged; register the `params_hash`. Confirm `refuse_clobber` no longer refuses (a dry `--controls-only` under the new name writes a new payload, not over the old one).

## 2. Run
Controls first over all four fixtures (gate); then the 41 surfaces from the target DataFile. `link_probe` once per surface; one HEAD per link, asserted from request counts per host in the RESULT. Whole bodies staged then promoted (§1.1). Observations and Findings as events on a new shard; project; `observed_on_missing_document: 0`; `error_class_unknown` reported.

## 3. Gate (the one gate of this task)
- Control gate over four fixtures, every rule its pre-registered verdict, `unknown` = 0.
- Re-derivation byte-identical for this cycle **and** for all four prior payloads (286, 33, 437, 33) under their own rules and params.
- `tests/test_scan_hygiene.py` green after the run: no uncited fixture output, `git status --porcelain corpus/` shows only this cycle's promoted bodies and events.
**Failure writes no Results and no page: report and stop.**

## 4. Register (cycle-suffixed only, through `scripts/cycle_results.py`)
Per leg: pass / fail / error / not_applicable / applicable_n / pass_rate / wilson_lo / wilson_hi / control_fired, all `_<cycle>`. Cycle: surfaces, findings, observations, control_findings, rederived_findings, agencies_unobservable, agencies_with_no_admitted_surface, hosts_refusing_or_unreachable, requests_per_host (one Result per host), error_class counts (one per class). A12 per host, labelled candidate. Matrix DataFile `scan_matrix_<cycle>`. No composite, no ranking.

## 5. Status, figures, page
`framework_writeback_measured.py --task <this file>` (DD-055 rule; expect no status change unless a leg that was all-error now has an observable surface, or vice versa). `figures.py` for this cycle into `figures/<cycle>/`, registered as Figure artifacts with `CONTAINS` edges, F1 to F4 as in the eda task; **F4 gains a fourth snapshot only if a status moved.** Add **F5 `cycle_over_cycle`**: for each leg, cycle 1 and cycle 2 pass rates with intervals side by side, `k/n` printed, and A1 and A3 visibly marked as **rule changed (v2→v3, v3→v4): not comparable** so the reader cannot read a difference there as a change in the host. The page inlines all five; footer keeps the non-claims and adds the cycle-2 request counts per host.

## 6. Report
RESULT `cc_tasks/2026-09-07_scan_run_2_RESULT.md`: gates first; request counts per host against cycle 1; the matrix; per-leg rates with intervals; what moved between cycles and whether any of it is attributable to the instrument rather than the host (A1/A3 are); indicators whose status changed and why; anything a surface did that no fixture anticipated (`v4` candidates, named not fixed); every premise this task got wrong. `python -m pytest tests/ assessment/`, `seldon verify`, `git diff` empty on protected paths. `seldon cc complete`; commit, push.

**SEQUENCING:** §1.1 → §1.2 → §1.3 → §1.4 → §1.5 → §2 (controls first; hard stop on control failure) → §3 (hard stop) → §4 → §5 → §6.
