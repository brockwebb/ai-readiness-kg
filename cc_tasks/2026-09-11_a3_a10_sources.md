# CC Task — A3 and A10 get their sources: no check in the published report rests on an uncited indicator

**Date:** 2026-09-11
**Project:** ai-readiness-kg
**Authored by:** Desktop session, OODA on `2026-09-11_l0_report_cycle4_revision_RESULT.md` §6 and §11 item 2.
**Fulfils:** its own ResearchTask (`seldon cc register`). Runs before publish. The repo's stated invariant is that every assertion is citable by a stranger; the traceability table shows two of the report's seven checks reach no source document, and one of them (A3, bulk download) is the only product check whose pass rate is worth reading.
**Spend:** zero model calls. **Network: acquisition only**, limited to the hosts of the four documents below. No federal host in the frame is contacted; the socket counter asserts it.

**Prior art, so the sources are not invented.** Both indicators have named, public, standards-grade sources:
- W3C, *Data on the Web Best Practices* (Recommendation, 31 Jan 2017). Persistent URIs as identifiers (the deep-link property A10 measures) and bulk download / making data available in bulk (A3) are each a numbered Best Practice. CC verifies the exact BP numbers from the document; the task file does not type them.
- Wilkinson et al. (2016), *The FAIR Guiding Principles for scientific data management and stewardship*, Sci. Data 3:160018, CC-BY. F1 (persistent identifiers) speaks to A10; A1 (retrievable by a standardised, open protocol) speaks to A3.
- OMB M-13-13, *Open Data Policy: Managing Information as an Asset* (2013), and its Project Open Data schema, which name bulk download as a required distribution form. Public, federal, and the frame's own policy lineage; the fss-policy-kg may already hold it.
- Berners-Lee, *5-star Open Data* (5stardata.info). Tertiary; admit only if the admission path accepts a web page as a Document with provenance, otherwise skip and say so.

**Decisions taken here (operator overrides later):**
1. **Documents enter through the repo's existing admission path**, whatever creates a `Document` node with provenance (URL, retrieval date, hash) in this repo. No ad hoc `CREATE`. If no such path exists for a non-literature document, §1 stops and the RESULT says what the path would need; a source admitted by hand is not citable by a stranger.
2. **Edges are the ones the traceability chain already uses**: `AssessmentIndicator -[:EVIDENCED_BY]-> Document`, with the specific section or best-practice identifier recorded on the edge or the Document as the chain records it for A4 and A5 today. Follow the existing shape; do not invent a new property.
3. **Both indicators reach at least two sources**, one standards body (W3C DWBP) and one peer-reviewed (FAIR), with OMB M-13-13 as the federal-policy anchor for A3. Extraction of `Definition` nodes runs if and only if the admission path runs it for every admitted document; it is not run specially for these.
4. **The report changes only if it carries a per-check source list.** If a section or appendix lists each check's sources, A3's and A10's rows are filled by tag or reference the same way the others are, and the PDF is rebuilt through `make report-pdf`. If no such list exists, the report is not touched; the citation lives in the graph and the RESULT says so. No new section.
5. **`scripts/report_traceability.py` becomes a test** with the seven-leg table as its expected shape: every leg ≥ 1 construct, ≥ 1 measurement spec, ≥ 1 rule, ≥ 1 source document. The arrow directions that were reversed on first writing are pinned by that test.

**Zero edits to:** rule modules, harness runtime, stored payloads, prior Results, prior RESULTs, cycle evidence, targets, figures, existing `Document` nodes, report prose beyond decision 4.

**Immutable once written. Glob `2026-09-11_a3_a10_sources_ADDENDUM*.md` before starting and again before §3.**

---

## 1. Decision 1. Find the admission path; admit the documents. Stop if any admission requires bypassing the path.
## 2. Decisions 2, 3, 4, 5. Edges, the per-check list if one exists, the traceability test.
## 3. Gate (the one gate of this task)
Traceability test green: all seven legs reach ≥ 1 source document, A3 and A10 ≥ 2; every admitted `Document` carries URL, retrieval date and hash; `EVIDENCED_BY` edges follow the existing shape; socket counter shows only the acquisition hosts; if the PDF was rebuilt, its numeral-multiset gate and the tag-coverage lint are green and the page count is reported; `make gate-task`; `make gate-full` detached, logged, polled; `seldon verify`; protected paths.
**Failure writes no Documents and no edges: report and stop, RESULT with the block on top, commit, push.**
## 4. Report
RESULT `cc_tasks/2026-09-11_a3_a10_sources_RESULT.md`: the documents admitted with their identifiers and the exact best-practice or principle cited for each indicator; the traceability table after; whether the report carries a per-check source list and what was done; every premise wrong. `seldon cc complete`, commit, push. Final message states the seven-leg source counts and whether the push succeeded.

**SEQUENCING:** §1 (stop on a bypass) → §2 → glob addenda → §3 (detached, logged, polled) → §4 → push.
