# CC Task: G4 is narrowed to what its rule measures and its sources define; both G4 Issues close

**Date:** 2026-09-21
**Project:** ai-readiness-kg
**Authored by:** Desktop session, on the operator's ruling of 2026-09-21: "narrow it and close it."
**Implements:** "every assertion must be citable by a stranger" (`CLAUDE.md`). Follows `cc_tasks/2026-09-21_g4_resourcing_reissue_RESULT.md` §0 Part A.
**Framework layer served (DN-005 §5 rule 1):** §2.1, the validity layer.
**Fulfils:** its own ResearchTask. Hand-dispatched (poller stopped by `.seldon/DISPATCH_STOP`).
**After:** none
**Spend:** zero pipeline model calls; this CC session is estimated at 5 to 8M tokens on Sonnet, unmeasured. **Network:** none beyond `git push`.

**HEADLESS NOTICE.** No next turn. Wait on every detached command with ONE blocking bash call (`until grep -q EXIT= <log>; do sleep 20; done`, 10 minute timeout, repeated as needed). Never poll turn by turn. Read files with targeted `grep`/`sed` ranges.

---

## The ruling

`RULE-G4-v1` measures one thing: issuing authority carried as machine-readable metadata (its own docstring says so). The previous task found that "statutory mandate" has no defining source, and that the clause about AI rankers is the framework's own inference. The operator's ruling: G4's text is narrowed to what is measured and sourced. The rule, the tier, every Finding and every verdict are untouched.

## Decisions

1. **G4's authored text** (the skeleton row in `docs/crosswalk/usafacts_operationalization_skeleton.md`: name, statement, and any rationale cell) states only: the issuing authority of a data product is carried as machine-readable metadata on the product, as DCAT-US defines the fields (`publisher`, `bureauCode`, `programCode`; use the locators the previous task wrote). Remove "statutory mandate". Remove every sentence claiming what AI rankers, retrievers or models need or do with it; G4 makes no benefit claim. Keep the statistical-versus-administrative provenance clause only if the rule measures it; if it does not, remove it too and keep the two FCSM citations out of G4's evidence cell. Read `rule_g4.py` to decide; quote the lines in the RESULT.
2. **Evidence cell:** keep only documents that support the narrowed text, each with its place and quote. Drop the rest from G4's cell (their `EVIDENCED_BY` edges go with them through the normal rebuild; `save` will report the edge delta, which is expected here and is the one case where a negative delta is correct; if `save` refuses it, stop and report, do not use `--force`).
3. **Path:** skeleton → `build_framework_graph.py` → `framework_writeback.save` → `build_projection.py` (full) → round-trip test → report, PDF and site rebuild. The release date must not move (the two-build guard from the previous task proves it). The version string is not changed: operator ruling, a wording correction to one indicator is not a new release.
4. **Close both open G4 Issues** with this closing note, verbatim: "Closed by narrowing G4 to the measured, sourced claim. Literature reviewed 2026-09-21, recorded here and not cited in the framework because G4 no longer makes a benefit claim: Wan, Wallace and Klein, ACL 2024 (models weight relevance and largely ignore credibility cues); Schuster et al., ACL 2026 (models prefer government and institutional sources when the source is visible, reversible by repetition); Ge et al., IJCAI 2025 (source metadata in context improves conflict resolution at generation). No study found tests whether catalog metadata reaches a model's context."
5. **Tests:** update `tests/test_g4_locators_and_progress_drift.py` and `tests/test_g4_resourcing_reissue.py` only where they pin quotes or documents this task removes; every remaining quote still passes the grounding test.

**Write set:** the skeleton (G4's row), `framework/ai_readiness_framework.json` (through `save`), rebuilt `docs/`, `docs/design/mcp_over_the_graph.md` if counts move (regenerated by `mcp/airkg_doc.py`), the two test files, `scripts/check_protected_g4_narrowed.sh`, `events/` and `seldon_events.jsonl` by their writers, the RESULT. `assessment/harness/scan/rules/`, `state/`, payloads, Findings: byte-identical. `score.py` output quoted before and after, identical.

**Immutable once written.**

## Gate and report
`make gate-fast`, then `make gate-full` detached with `-rs`, `seldon verify`, protected paths. RESULT `cc_tasks/2026-09-21_g4_narrowed_RESULT.md`, under 40 lines: G4's text before and after; the evidence cell before and after; `save`'s delta line; the `rule_g4.py` lines that decided clause 3; Issue ids closed; gate table; premises this file got wrong. `seldon cc complete`, commit, push.

**SEQUENCING:** decisions 1 to 3 → 4 → 5 → gate → report → push.
