# CC Task — scan-run-3: the first cycle over the whole frame, 16 + 3

**Date:** 2026-09-08
**Project:** ai-readiness-kg
**Authored by:** Desktop session, OODA on `2026-09-08_scan_frame_fss_RESULT.md` (gate PASS, `4f1370b2` completed; premises re-read from the graph: 19 hosts, 65 surfaces, 24 carried, 7 agencies pending declaration, 38 per-host pre-flight Results, zero open conversion-gap tasks).
**Fulfils:** its own ResearchTask (registered by `seldon cc register`). Precedes `report-draft` (`2517cda8`).
**Spend:** zero model calls. **Network:** the 19 hosts of `scan_targets_fss_2026-09` and no other. Identified UA `0.2` (DD-060), 1 req/s per host, robots obeyed, one worker, shared `link_probe`, one HEAD per link. Report the request count per host.

**Premise (verified):** targets DataFile `scan_targets_fss_2026-09` (65 rows: 19 home, 19 well-known, 17 flagship, 10 machine; every row `operator declaration` or `roster host`; Tier C rows flagged `tier: C`); `CURRENT` = harness-v4 (A10-v3, A8-v3, A1-v3, A3-v4); five control fixtures; `finding_identity: 2`; seven prior payloads re-derive byte-identically; hygiene suite green (1,592 passed). `params.tier0.legs` **does not exist yet** (frame RESULT §9.2). Three hosts (BLS, BTS, SSA) have refused the identified client on four measurements; `www.federalreserve.gov` serves no `robots.txt`; `www.eia.gov` answered 503 on its home twice; one EIA surface is unadmitted because `robots.txt` disallows it for this UA.

**Decisions taken here (operator overrides later):**
- Tier C rows are judged on tier-0 legs **only**; no Finding is created for a Tier C row on any other leg (not `not_applicable`: absent, because the leg was never asked). The tier-0 set is `params.tier0.legs = [A4, A5, A10, A11-declared, A12, G1-D]`, declared before the run, A12 reported beside the others as candidate.
- The 7 pending agencies run on home and probes only. Their thin rows are a measurement, reported as such, not padded.
- EIA 503 is measured, not pre-judged: whatever the collector sees is the observation.

**Zero edits to:** shipped rule modules (new behaviour is a new module), the targets DataFile, events, the G1 harness, `assessment/cq/*.yaml`, framework content beyond the `measured` write-back, `params.yaml` beyond `cycle.*`, `tier0.legs` and the `blind_links` fixture entry, any prior cc_task or RESULT.

**Immutable once written. Changes require a new task file or an `_ADDENDUM-NN.md` sibling. Glob and read all siblings `2026-09-08_scan_run_3_ADDENDUM*.md` before starting, and glob again before §2.**

---

## 1. Pre-flight (fixtures only, no federal host)
1. `params.tier0.legs` declared as above; the figure test gains the ADDENDUM-01 assertion (no Tier C row in F2; no Tier C point in F1 or F5 for a leg outside `tier0.legs`); `figures.py` renders a separate Tier C block.
2. **Blind links are `error`, not `fail`** (harness-v4 §13.1): `RULE-A1-v4` and `RULE-A3-v5` treat a link probe whose class is blind as unobserved for that link; if every link on a surface is blind the leg is `error`; if some are, the verdict is computed over the observed links and the count of blind links is on the Finding. Fixture `resets_links_only` (home and content served, every discovered link resets on HEAD): pre-registered verdicts A1 `error`, A3 `error`, everything else as `passes_all`. Six fixtures.
3. `cycle.name: scan_<UTC date at this step>`; `cycle.targets: scan_targets_fss_2026-09`; register `params_hash`. `refuse_clobber` confirmed.
4. Control gate over six fixtures (hard stop).

## 2. Run
Controls first (done in §1.4; re-run under the final params hash). Then the 65 surfaces: Tier A on all `CURRENT` legs, Tier C on `tier0.legs` only. Evidence staged then promoted on publish. Observations and Findings on a new shard; project; `observed_on_missing_document: 0`; `error_class_unknown` reported.

## 3. Gate (the one gate of this task)
- Six-fixture control gate under the final params, every rule its pre-registered verdict, `unknown` = 0.
- Byte-identical re-derivation for this cycle and all seven prior payloads under their own rules and params.
- Tier C: zero Findings on any Tier C row outside `tier0.legs`.
- Hygiene suite green; `git status --porcelain corpus/` shows only this cycle's promoted bodies and events.
**Failure writes no Results and no page: report and stop, RESULT with the block on top, commit, push.**

## 4. Register (cycle-suffixed only, through `scripts/cycle_results.py`)
Per leg (Tier A denominators): pass / fail / error / not_applicable / applicable_n / pass_rate / wilson_lo / wilson_hi / control_fired / blind_links. Tier C per leg separately (`scan_tierc_<leg>_*`). Cycle: surfaces, hosts, findings, observations, control_findings, rederived, agencies_refusing_identified_client, agencies_pending_declaration, hosts_without_robots_txt, requests_per_host, error classes. A12 per host, candidate. Matrix DataFiles `scan_matrix_<cycle>` (Tier A) and `scan_matrix_tierc_<cycle>`.

## 5. Status, figures, page
`framework_writeback_measured.py --task <this file>`. F1 to F5 for this cycle plus **F6 `tier_c_reference_hosts`** (three hosts × six tier-0 legs, verdict cells, `k/n` not applicable; no rate). F4 gains a snapshot only if a status moved. F5 now spans cycles 1, 2, 2-rj1, 3 with rule-change markers (A1, A3, A10, A8 differ from cycle 2). Page regenerated; footer names the frame (16 + 3), the 7 thin agencies, and the non-claims.

## 6. Report
RESULT `cc_tasks/2026-09-08_scan_run_3_RESULT.md`: gate first; request counts per host; Tier A matrix and rates; Tier C block; what moved against cycle 2 and whether the instrument or the host moved it (A1/A3/A8/A10 rule changes are the instrument); the refusal-consistency finding (three hosts, five measurements) stated as a finding about accessibility; EIA 503 as observed; every premise this task got wrong; cycle-4 needs named not built. `python -m pytest tests/ assessment/`, `seldon verify`, `git diff` empty on protected paths. `seldon cc complete`; commit, push.

**SEQUENCING:** §1.1 → §1.2 → §1.3 → §1.4 (hard stop) → glob addenda again → §2 → §3 (hard stop) → §4 → §5 → §6 → push.
