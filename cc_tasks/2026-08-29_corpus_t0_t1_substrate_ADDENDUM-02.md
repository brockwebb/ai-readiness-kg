# ADDENDUM-02 to 2026-08-29_corpus_t0_t1_substrate.md — availability is a queue; close criteria amended

**Date:** 2026-08-29. Trigger: operator directive + mid-run report (OpenAlex daily quota, 29/178 T0; GROBID unavailable; Docling failures → pypdf fallback). Binding rule now methodology §7.10: **availability states never halt lanes; only validity gates halt work.**

## 1. Close criteria amended

The task closes with **T1 complete** and **T0 at whatever coverage exists at close**. T0 completeness is NOT an exit criterion. Required instead:
- `python -m kg.biblio resume` — idempotent, resumable from `harvest_error: retryable` state, safe under quota (backoff + provider ladder already built). One command finishes T0 whenever run; no session context needed.
- Coverage table in the RESULT: resolved / retryable / partial / blocked, per provider.
- §2.2 coupling ranking and §2.3 `t2_priority`: compute on current coverage, label **provisional (T0 coverage n/178)**, and make `resume` recompute both automatically at its completion. An unusable ranking honestly labeled and auto-refreshed beats a held task. Crosswalk-demand component of `t2_priority` is coverage-independent — report it now regardless.

## 2. Operator pickup list (the "on me to get" queue)

Add `docs/corpus/operator_pickup.md`, projected not hand-written: every doc in `acquisition_blocked`, `manual_download_needed`, or `fidelity: degraded` where a better source plausibly exists, ordered by `t2_priority` (provisional ordering acceptable, labeled). Columns: doc/candidate, state, why blocked, best-known URL, priority. Regenerated by the same projection that builds `manifest_table.md`. Currently known members: Commerce guidance (Cloudflare), any Docling-failed docs, T2-priority items from the candidates list that are closed-access.

## 3. Degraded docs proceed at full speed

`fidelity: degraded` docs get complete T1 treatment (chunks, FTS, embeddings) and are eligible for T2 when that day comes; the flag propagates to their chunk records and any downstream derivation. When a clean copy lands in the inbox: normal supersession (`content_update`), then re-convert + re-index **that document only**, marking its prior derivations stale. No lane waits for a clean copy.

## 4. Defect fixes folded in

- Log-bomb guard: any converter/tool exception message is truncated to 2K chars before logging (the Docling page-dictionary dump wrote 230K lines and killed the process); raw exception preserved to a per-doc file, not the shared log. Test: seeded oversized exception truncates.
- GROBID: do NOT install a Java runtime under this task (no mandate — correct call). DOI-less docs use title search; record `biblio_method` per doc. If title-search yield is poor, GROBID-via-Docker is a candidate for a future task, noted with the yield number that would justify it.
- The retracted `bibliographic_partial` mislabeling: confirm no residue — a transient failure state must never be recorded as a document property. Guard: `bibliographic_partial` may only be written by the code path that positively confirmed metadata absence at the source, not by any error handler.

## 5. Unchanged

Everything else in the base task and ADDENDUM-01, including the §-0.5 outcome (ledger-sync fix stands) and zero `model_stub` spend.
