# CC Task — the self row: the instrument's six host-level checks against the host that publishes it

**Date:** 2026-09-13
**Project:** ai-readiness-kg
**Authored by:** Desktop session, OODA on `2026-09-12_publish_l0_RESULT.md` §3, §7, §9 item 1. Pages enabled by the operator on 2026-09-13.
**Implements:** DN-002 decision 2; under DD-001. Runs after `2026-09-13_ephemeral_provenance.md` and its addendum.
**Fulfils:** its own ResearchTask (`seldon cc register`).
**Spend:** zero model calls. **Network: the published authority only** (`brockwebb.github.io`), with the identified client. No federal host and no reference host; the socket counter asserts it.

**Decisions taken here (operator overrides later):**
1. **The scan runs through the normal harness path**: a one-host frame naming `https://brockwebb.github.io/ai-readiness-kg/` as the site and the authority root as what RFC 9309 makes it, `run.py` as any cycle runs, cycle name `self_2026-09-13`, verdicts registered `self_l0_<leg>_self_2026-09-13`, payload dropped at `state/self_l0_self_2026-09-13.json`, which the site builder already reads. No rule, collector or manners change; a self-scan that needs the harness bent is not a self-scan.
2. **Every Finding records the URL it actually read.** For A4, A5, A11-declared and A12 that is a root path on `brockwebb.github.io`; the row states, per leg, which authority answered and whether that authority is this publication. If the operator has since created the user-site repository and its root serves `robots.txt` and `llms.txt`, that is what the legs read and the row says so; if the root still answers 404, the legs return what they return and the row says so. Nothing is inferred from which case it is; the payload is the record.
3. **A `fail` stays visible.** Nothing under `docs/` is changed to improve a verdict after it is measured. A row a publisher fixes after scanning is what the instrument measures publishers for.
4. **The live-host clause of the publish gate is now reachable and runs**: every relative `href` on the index and every `<loc>` in the sitemap resolves on the live host, HTTP 200, with the identified client. The `data/` files are fetched and byte-compared to the tree.
5. **The page renders the row** from the payload; `test_the_self_row_is_measured_or_says_it_is_not` flips to its measured branch with no edit to the test.

**Zero edits to:** rule modules, harness runtime, manners, stored payloads, prior Results, prior RESULTs, figures, section prose, the skeleton, the record, `corpus/`, `docs/robots.txt`, `docs/llms.txt`, `docs/sitemap.xml`.

**Immutable once written. Glob `2026-09-13_self_row_ADDENDUM*.md` before starting and again before §3.**

---

## 1. Decision 4 first: confirm the host serves the tree. Stop if any index link or sitemap entry does not resolve; the RESULT says which.
## 2. Decisions 1, 2, 3, 5.
## 3. Gate (the one gate of this task)
Six verdicts registered under `self_l0_*` names with the payload on disk; every Finding carries the URL read; the row renders with per-leg authority stated; index and sitemap resolve on the live host and `data/` byte-matches; socket counter shows the published authority only; both invariant readings 0 on the self payload; `make gate-task`; `make gate-full` detached, logged, polled; `seldon verify`; protected paths.
**Failure writes no Results and no page change: report and stop, RESULT with the block on top, commit, push.**
## 4. Report
RESULT `cc_tasks/2026-09-13_self_row_RESULT.md`: the six verdicts with the URL each read; what the authority root served; every premise wrong. `seldon cc complete`, commit, push. Final message states the six verdicts and the live URL of the row.

**SEQUENCING:** §1 (stop on a dead link) → §2 → glob addenda → §3 (detached, logged, polled) → §4 → push.
