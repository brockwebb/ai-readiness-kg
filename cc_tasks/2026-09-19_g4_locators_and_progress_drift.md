# CC Task: G4's two sources get a locator or an honest "none found", and `docs/progress/` is compared to the record it is a view of

**Date:** 2026-09-19
**Project:** ai-readiness-kg
**Authored by:** Desktop session, from `cc_tasks/2026-09-19_resnapshot_rj4_RESULT.md` §5 premises 8 and 12 and its open list items 2 and 4, as carried by `docs/design/2026-09-19_DN-007_operator_rulings_and_state.md` §3.
**Implements:** "every assertion must be citable by a stranger" (`CLAUDE.md`); invariant 3 (no grounding span, no write) applied to an evidence cell; the publication-guard rule of `cc_tasks/2026-09-16_publication_guards.md` (every generated consumer is compared to its source).
**Framework layer served (DN-005 §5 rule 1):** §2.1, the validity layer (the crosswalk indicator → primary source), and the views of the framework.
**Fulfils:** its own ResearchTask (`seldon cc register`). Launched by the dispatcher. Serves one instance of open task `34ccb843` (staleness gate for every committed build product); does not close it.
**After:** 2026-09-19_seldon_hygiene_superseded_cadence_after (the header that task introduces; until it lands the edge is written by `seldon_task_chain` at registration, and this file's gate needs that task's `seldon verify` fix to exit 0)
**Spend:** zero model calls. **Network:** none beyond `git push`.

**HEADLESS NOTICE.** Under `claude -p` there is no next turn. Every command longer than two minutes is detached with its `EXIT=` line inside the `bash -c` string, logged, and polled in this turn.

---

## 0. What was checked before this was written

**0.1 G4.** `framework/ai_readiness_framework.json`, node `ind:G4`: `evidence_raw` is `` `statistical-policy-working-paper-46-data-quality-assessment`; `fcsm-19-01-transparent-reporting-for-integrated-data-quality` `` with nothing after either id. The two `EVIDENCED_BY` edges carry `doc_id` only. The report quotes G4 since the re-snapshot, so its sources appendix carries two G4 rows whose Locator cell is empty. The convention a filled cell follows is visible on A1, A8 and B3: the doc id, then in parentheses the place in the document and the sentence quoted verbatim (A1: `` `w3c-dwbp-2017` (BP 12, ...: "make data available in a machine-readable, standardized data format ...") ``). The authored source of the cell is `docs/crosswalk/usafacts_operationalization_skeleton.md`; `scripts/build_framework_graph.py` regenerates authored cells from it and `scripts/framework_writeback.py::save` is the only writer of the record.

**0.2 The population this task does not fix.** By a text test for a place-and-quote in `evidence_raw`, 12 of 49 indicator nodes carry a locator and 37 do not. G4 is the one the report currently quotes, which is why it is the one a reader meets. The other 36 are recorded as ResearchTask `93d28c6e`; this task does not touch them and the RESULT does not claim the evidence layer is located.

**0.3 `docs/progress/`.** Generated by `scripts/framework_progress.py --cycle <cycle>`. `tests/test_scan_run_2.py` ties it to the cycle of record's denominator and nothing ties it to the framework record: the re-snapshot's regeneration moved counts that had been wrong on the page since 2026-09-11 and no test had noticed. `tests/test_publication.py` is where the generated-consumer comparisons of `2026-09-16_publication_guards` live; `docs/progress/` is not among them.

Prior art for 0.3: the check is the standard "generated file is up to date" gate, regenerate into a scratch directory and byte-compare with the committed copy (`go generate` followed by `git diff --exit-code`; Bazel `diff_test`; `make -q`). Nothing to design. For 0.1: a pinpoint citation is what every citation standard requires of a quoted or specific claim (Bluebook rule 3.2; APA 7 §8.13); a source cited without one is cited as general support, and the appendix should say which it is.

**Decisions taken here (operator overrides later):**

1. **For each of the two documents, find the passage that supports G4's construct as the indicator states it** (issuing authority, statutory mandate, statistical-versus-administrative provenance, carried as metadata), reading the converted substrate on disk under `corpus/`. A locator is a place a stranger can turn to (section number and heading, page of the PDF, table number) plus the sentence quoted verbatim. **The quote must pass `kg/extraction/grounding.py` against that document's substrate**, called from a test, not checked by eye.
2. **Three outcomes are legal per document, and the third is not a failure.** (a) A passage supports the construct: the cell gets `(place: "quote")`. (b) A passage supports one clause only: the cell says which clause, in the cell. (c) No passage supports it: the cell gets no invented locator; the RESULT says so with what was searched (the terms, the sections read), and an Issue is filed proposing the `EVIDENCED_BY` edge be reviewed. Neither document was written about AI rankers; the final clause of the indicator's text ("the trust signal AI rankers need ...") is the framework's own inference and no locator is sought for it. **A locator is never written to make the appendix look complete.**
3. **The edit lands in the skeleton's G4 evidence cell, then `build_framework_graph.py`, through `framework_writeback.save`, then a projection** (`scripts/load_framework_graph.py`), then `tests/test_framework_projection_roundtrip.py` green, then the report rebuild so the appendix's two G4 rows carry the locator or the words `general support; no pinpoint` for outcome (c). `save` must report zero dropped nodes, edges or `counts` keys; `--force` is not used. If the appendix builder reads the locator from somewhere other than the evidence cell, the RESULT says where and the edit goes there instead, by the same single-writer path.
4. **`docs/progress/` joins the generated-consumer guard.** A test regenerates the page(s) with `framework_progress.py` for the cycle named in `publication.yaml` into `tmp_path` and compares byte for byte with the committed files; on a difference the failure message prints the first differing line of each file and the regeneration command. The generator must therefore be deterministic: if it stamps a wall-clock time or an unordered iteration into the page, that is fixed in the generator (the stamp comes from the record or the cycle, not from `now`), not excused in the test. The test runs in the fast tier.
5. **`34ccb843` is read, not closed.** The RESULT lists every committed build product that task names, marks which now have a regenerate-and-compare guard (figures, the `2026-09-16` consumers, `docs/progress/` after this task) and which do not. If the list of unguarded products is empty the RESULT says so and Desktop closes `34ccb843`; CC does not.

**Write set:** `docs/crosswalk/usafacts_operationalization_skeleton.md` (G4's evidence cell only), `framework/ai_readiness_framework.json` (through `save` only), `docs/reports/` rebuilt products that carry the G4 appendix rows, `docs/progress/` only if decision 4's determinism fix changes its bytes, `scripts/framework_progress.py`, `tests/` (the grounding test for the two quotes; the drift test), `events/` and `seldon_events.jsonl` by the writers that own them, the RESULT. `state/`, `corpus/`, `assessment/harness/scan/rules/`: byte-identical. No rule, payload or Finding is touched, so no verdict, score or rank can move; the RESULT asserts that by quoting `score.py` output before and after.

**Immutable once written. Glob `2026-09-19_g4_locators_and_progress_drift_ADDENDUM*.md` before starting and again before §3.**

---

## 1. Decisions 1 to 3. The grounding test first, failing on an empty quote; then the reading; then the single-writer path, the projection, the round-trip test, the report rebuild.
## 2. Decisions 4 and 5. The drift test first, and its negative control quoted in the RESULT: one count edited by hand in a scratch copy of the page, the test's failure message shown.
## 3. Gate
`make gate-task` is not required (no rule module, registry or re-derivation engine changes); `make gate-fast`, then `make gate-full` detached with `-rs` before the push, `seldon verify`, protected paths. Every gate row quotes passed, skipped, xfailed, deselected, wall clock and log path. Failure ships nothing: report and stop.
## 4. Report
RESULT `cc_tasks/2026-09-19_g4_locators_and_progress_drift_RESULT.md`: §0 per document, the outcome (a, b or c), the locator and quote or the search that found none, and the grounding test's output; §1 the appendix's two G4 rows before and after; §2 `save`'s delta line and the round-trip test; §3 the drift test, its negative control, and any determinism fix with its diff; §4 the `34ccb843` table; §5 `score.py` before and after; §6 every premise this task file got wrong; §7 the gate table, tier named. `seldon cc complete`, commit, push.

**SEQUENCING:** glob addenda → §1 → §2 → glob addenda → §3 → §4 → push.
