# CC Task — two standing guards: no re-judgement off the log, and the report says when its snapshot is superseded

**Date:** 2026-09-14
**Project:** ai-readiness-kg
**Authored by:** Desktop session, OODA on `2026-09-14_rejudgements_on_the_log_RESULT.md` §2, §7 items 1, 2, 4.
**Implements:** DN-003 decision 6 and DN-004 decisions 2, 3 (`docs/design/2026-09-14_DN-004_report_snapshot_policy.md`); under DD-001, DD-065.
**Fulfils:** its own ResearchTask (`seldon cc register`). Two guards, one gate: both exist to keep the graph, the log and the published report in agreement without a Desktop session noticing a drift by hand.
**Spend:** zero model calls. **Network: none.**

**Decisions taken here (operator overrides later):**
1. **`make guards` gains the DN-003 decision 6 test**: every payload in `state/` for which `publish.is_rejudgement` holds has all of its Findings on the log, attributed to it by `Finding.cycle`, with a `SUPERSEDES` edge to its predecessor. It passes today at 14 of 14 and would have failed continuously from 2026-09-10 to 2026-09-14. A measured payload in `state/` with no Observations on the log is the same class and is asserted by the same test.
2. **The report's version block carries the supersession line** (DN-004 decision 2), generated by the report builder from a query: snapshot `scan_2026-09-10_rj2`, successor `scan_2026-09-10_rj3`, verdict moves 0, reason-only changes 3, generation 10. `results_tagged.json` carries the same fields. A test asserts the line equals the query's answer at build time and that the numerals in it pass the multiset gate (they are generated, scoped out of the bare-numeral lint like the matrices).
3. **The build refuses when a successor moves a published number** (DN-004 decision 3): before rendering, the builder computes every tagged Result and every published matrix cell from the successor cycle in a temporary tree and byte-compares to what the snapshot produced; any difference is a refusal naming the numbers. Today it passes (rj3 moved nothing numeric). The refusal message names the DN-002 revision task as the next step.
4. **`scripts/publish_rejudgements.py --project-once`** (RESULT §7 item 4): a batch of N publications projects once at the end; per-cycle projection stays the default for a live cadence. A test asserts the end-state graph is identical either way on a two-cycle fixture.
5. **`state/scan_2026-09-07b_rj2.json`'s self-name is left as is** (immutable payload, disagreement carried on the events). Not this task's.

**Zero edits to:** rule modules, manners, stored payloads, the log, prior Results' values, states and edges, prior RESULTs, figures, section prose, the skeleton, the record, `corpus/`, `docs/` beyond the report's version block, its PDF, and `results_tagged.json`.

**Immutable once written. Glob `2026-09-14_standing_guards_ADDENDUM*.md` before starting and again before §3.**

---

## 1. Decisions 1, 3. Stop if decision 3's comparison finds any published number moved under rj3: that is a re-snapshot condition and the RESULT names the numbers before anything is built.
## 2. Decisions 2, 4. Rebuild the report and PDF.
## 3. Gate (the one gate of this task)
Decision 1's guard green at 14 of 14 re-judged and 8 of 8 measured; decision 3's comparison 0 moved; the supersession line present in the markdown and the PDF and equal to the query; PDF numeral-multiset gate and bare-numeral lint green; `--project-once` fixture test green; page counts unmoved or re-registered; `make guards`; `make gate-task`; `make gate-full` detached, logged, polled; `seldon verify`; protected paths.
**Failure writes no PDF and adds no guard: report and stop, RESULT with the block on top, commit, push.**
## 4. Report
RESULT `cc_tasks/2026-09-14_standing_guards_RESULT.md`: the supersession line as rendered; decision 3's comparison summary; every premise wrong. `seldon cc complete`, commit, push.

**SEQUENCING:** §1 (stop on a moved number) → §2 → glob addenda → §3 (detached, logged, polled) → §4 → push.
