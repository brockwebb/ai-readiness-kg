# CC Task — scan-run: measure the 41 surfaces under v2, controls first, history untouched

**Date:** 2026-09-07
**Project:** ai-readiness-kg
**Authored by:** Desktop session (OODA on `2026-09-06_scan_targets_RESULT.md`)
**Fulfils:** ResearchTask `scan-run` (`e3e38014`); predecessor `22fb59b2` completed.
**Premise (from the scan-targets RESULT, verified in that file's §4–§7):** 41 surfaces on `scan_targets_2026-09` (26 admitted documents + 14 synthetic well-known sets + 1 disallowed target not admitted); 3 hosts (`www.bls.gov`, `www.bts.gov`, `www.ssa.gov`) refuse the identified UA on every probe; `CURRENT` rules are 12 `v2` + 4 `v1` (A4, A5, A9 with `decision` on the spec; G1-D); A12 is `candidate` with `RULE-A12-v0` placeholder; F-UJI unavailable; `seldon verify` reports only the cross-graph `PRECEDES` collision (seldon `1ad92c2b`, fixed in the seldon repo in parallel).
**Spend:** zero model calls (asserted by the existing test). Network: the 14 hosts, identified UA, 1 req/s per host, robots obeyed. **One worker, not parallel CC:** at 1 req/s across 14 hosts the whole cycle is minutes; parallel workers buy nothing and add coordination risk. Parallel dispatch is for when the target list is tens of agencies, and that is stated here so it is not re-decided ad hoc.
**Zero edits to:** any rule module (`v1` or `v2` — a defect found here is a `v3`, next task), `params.yaml` except the additions §1 names, the target list, the G1 harness, `assessment/cq/*.yaml`, the vocabulary log, indicator content in the JSON except `measurement_status`.

**Immutable once written. Changes require a new task file or an `_ADDENDUM-NN.md` sibling. Glob and read all siblings `2026-09-07_scan_run_ADDENDUM*.md` before starting.**

---

## 0. What "measured" means here (DD-055)
`measurement_status: measured` on an indicator means: a cycle with fired controls produced, for that indicator's AUTO leg, at least one `pass`/`fail`/`not_applicable` Finding on an **admitted, observable** surface under `CURRENT` rules, with the evidence bytes committed and the Finding re-derivable. An `error` Finding does not count toward `measured`. A12 stays `candidate` regardless of what it observes.

## 1. Pre-run
- `RULE-A12-v1` from the pre-flight observations (`host_preflight`): `pass` if robots.txt permits the flagship path for this UA **and** the flagship GET returned 2xx/3xx/404; `fail` if robots permits and the host answered 401/403/429; `not_applicable` if robots disallows. Register it in `REGISTRY` and `CURRENT`; it judges a synthetic host surface, and its Findings are recorded but **excluded from every fraction** (candidate rule, per DD-054). Extend the two fixtures so the control gate exercises it.
- `params.yaml`: add `cycle.name: scan_2026-09-07` and `cycle.targets: scan_targets_2026-09`; confirm `manners.unobservable_statuses` and everything else is unchanged from the reviewed set (diff must show only these two keys; register the `params_hash`).

## 2. Run
Controls first (gate), then the 41 surfaces from the target DataFile — never a typed URL. Exactly one Finding per (surface, leg) for the 15 AUTO legs plus G1-D/G1-O where applicable, plus A12 per host. Whole bodies retained content-addressed and committed. Observations and Findings as events on a new shard; project with labelled Cypher; `observed_on_missing_document: 0`.

## 3. Gates
Control gate PASS; re-derivation gate PASS on this cycle (byte-identical) **and** on the two prior cycles under their own rules and params (history untouched — the test from `scan-targets` §1.3 must stay green); `kg.manifest verify` clean; the round-trip gate green after §5.

## 4. Register — instrument Results, for the first time
Denominators are stated on every Result, and unobservable hosts are never silently dropped:
- per leg: `scan_<leg>_pass`, `_fail`, `_not_applicable`, `_error` counts; `scan_<leg>_pass_rate` = pass / (pass + fail) over **observable admitted surfaces** with a Wilson 95% interval; `scan_<leg>_applicable_n`.
- per agency: the 15-leg verdict row (as a snapshot DataFile `scan_matrix_2026-09-07`, agencies × legs); `scan_agencies_unobservable` (3 expected) and `scan_surfaces_unobservable`.
- cycle: `scan_surfaces`, `scan_findings`, `scan_observations`, `scan_control_findings`, `scan_rederived_findings`, `scan_params_hash`.
- A12: `scan_a12_pass`, `_fail`, `_not_applicable` per host, labelled candidate in the description.
No composite. No ranking of agencies. Every Result carries `cycle: scan_2026-09-07` and the `params_hash`.

## 5. Status and page
Move `measurement_status` to `measured` for every indicator that meets §0; leave the rest `harness_built` and list them with the reason (all-error, or no applicable surface). Re-render the skeleton; round-trip test. Re-run `framework_progress.py`; the progress page gains the agencies × legs matrix (inline SVG, verdict classes as four fills, unobservable rows visibly distinct) and a per-leg pass-rate bar with intervals. Register the fractions.

## 6. Report
RESULT: `cc_tasks/2026-09-07_scan_run_RESULT.md`. Lead with the gates, then the matrix, then per-leg pass rates with intervals, then the indicators that did not reach `measured` and why, then anything a surface did that no fixture anticipated (these are `v3` candidates and next task's input — name them, do not fix them). State every premise this task got wrong. `python -m pytest tests/ assessment/`, `seldon verify`, `git diff` empty on protected files. `seldon cc complete`; move `e3e38014` to completed with this RESULT as evidence; commit, push.

**SEQUENCING:** §1 → §2 (controls first; hard stop on control failure) → §3 → §4 → §5 → §6.
