# CC Task — RULE-A12-v3 and the `unobserved_error` message: reason strings that describe the evidence, re-judged under generation 10

**Date:** 2026-09-13
**Project:** ai-readiness-kg
**Authored by:** Desktop session, OODA on `2026-09-13_self_row_RESULT.md` §6, §10 item 2; absorbs ResearchTask `c5c0d15d` (`_common.unobserved_error` prints the class note when the status was the cause), closed as superseded in §4.
**Implements:** DD-055 (a Finding's reason describes what was observed) and the rule-version discipline of DN-001's predecessor decisions (a rule change is a new version and a re-judgement, never an edit). Runs after `2026-09-13_self_cycle_promote.md`.
**Fulfils:** its own ResearchTask (`seldon cc register`).
**Spend:** zero model calls. **Network: none.** Re-judgement is over stored Observations.

**Why this task exists.** `RULE-A12-v2` tests `wrong_content_type` before `robots_status`, so a host whose `/robots.txt` answers 404 with an HTML error page is described as having served a robots.txt with the wrong content type. The verdict is right and the sentence is false. One published Tier A Finding carries it (`host:www.federalreserve.gov`, cycle 4 re-judged) and the self row carries another. `_common.unobserved_error` has the same shape of defect: the note names the error class when the status in `manners.unobservable_statuses` is what fired.

**Decisions taken here (operator overrides later):**
1. **`RULE-A12-v3`**: status is examined before content type; the reason for a 404 is the same sentence A4-v1 already uses ("no robots.txt served"), read from a shared constant so the two rules cannot drift; the wrong-content-type branch fires only on a 2xx. v2 stays in `REGISTRY`. A control fixture already exercises the 404-with-HTML case if one of the eight does; if none does, the loopback fixture gains that mode and the derived control table gains the row, expectation from collector dispatch (DD-061).
2. **`_common.unobserved_error`** prints the note for the branch that fired; a test feeds it (class `http_4xx`, status 403 in `unobservable_statuses`) and asserts the status note, and (class `resets_connection`, status none) and asserts the class note. Every rule that calls it turns over to a new version only if its reason text is inside the re-derivation comparison for that rule; the RESULT lists which rules turned over and which did not need to.
3. **Re-judge under generation 10**: every stored payload whose CURRENT-version set changed, names continuing each cycle's sequence (cycle 4 `_rj3`, self `_rj1`, others as their sequence dictates). A test asserts **zero verdict moves** across all of them: this generation changes sentences, not judgements. Any verdict move is a stop.
4. **Register nothing.** No Result value moves, so under "register only what moved" no Result is registered; the re-judged payloads and their comparison records are the deliverable. The report is not rebuilt; its snapshot stays `scan_2026-09-10_rj2` and its tagged Results stay `published`, because their values are unchanged and the judgement of record for the report's numbers is unchanged. The RESULT states which published Finding reasons are superseded by which `_rj3` Finding.
5. **Evidence, not fixtures, for the federalreserve case**: the RESULT shows the v2 and v3 reason strings side by side on the stored Observations.

**Zero edits to:** shipped rule modules (new versions are new files), harness runtime, manners, stored payloads, prior Results' values and states, prior RESULTs, figures, section prose, the skeleton, the record, `corpus/`, `docs/`.

**Immutable once written. Glob `2026-09-13_rule_a12_v3_ADDENDUM*.md` before starting and again before §3.**

---

## 1. Decisions 1, 2. The two changes, their tests, the fixture mode if needed.
## 2. Decisions 3, 4, 5. Stop on any verdict move.
## 3. Gate (the one gate of this task)
Zero verdict moves across every re-judged payload, with per-payload counts of reason-only changes; A12-v3 and A4-v1 share the 404 sentence by construction (test); `unobserved_error` branch test green; control gate green over all fixtures with `unknown` = 0; byte-identical re-derivation of every stored payload under its own versions; both invariant readings 0 on every new payload; no Result registered (asserted by count before and after); `make gate-task`; `make gate-full` detached, logged, polled; `seldon verify`; protected paths.
**Failure writes no payload: report and stop, RESULT with the block on top, commit, push.**
## 4. Report
RESULT `cc_tasks/2026-09-13_rule_a12_v3_RESULT.md`: per payload, reason-only changes by leg; the federalreserve and self-row sentences before and after; which rules turned over for decision 2 and why; every premise wrong. Close `c5c0d15d` via `seldon task close` with a pointer. `seldon cc complete`, commit, push.

**SEQUENCING:** §1 → §2 (stop on a verdict move) → glob addenda → §3 (detached, logged, polled) → §4 → push.
