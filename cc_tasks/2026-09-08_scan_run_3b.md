# CC Task — scan-run-3b: the first cycle over the whole frame, 16 + 3, second attempt

**Date:** 2026-09-08
**Project:** ai-readiness-kg
**Authored by:** Desktop session, OODA on `2026-09-08_scan_run_3_RESULT.md`. Supersedes `d261be8b` (scan-run-3), whose §1.1 and §1.2 stand on disk and are **not redone**: `params.tier0.legs` declared, `RULE-A1-v4`/`RULE-A3-v5` shipped, fixture `resets_links_only` shipped.
**Fulfils:** its own ResearchTask (`seldon cc register`). Follows `2026-09-08_a8_v4_blind_pointer_and_fixture_table.md` (its RESULT must exist with gate PASS; if absent or FAIL, stop at §0). Precedes `report-draft` (`2517cda8`).
**Spend:** zero model calls. **Network:** the 22 netlocs of `scan_targets_fss_2026-09` after §1.1, and no other. Identified UA `0.2` (DD-060), 1 req/s per host, robots obeyed, one worker, shared `link_probe`, one HEAD per link. Report the request count per netloc.

**Premise (verified against `state/scan_targets_fss_2026-09.json`):** the three Tier C `machine` rows carry `host: www.data.gov | www.nist.gov | www.gsa.gov` but URLs on `catalog.data.gov`, `data.nist.gov`, `open.gsa.gov`; those netlocs are not in `hosts[]`, so `manners.on_roster_host` would refuse them. The rows are `operator declaration 2026-09-08`: the operator chose the surfaces; the roster omitting their netlocs is Desktop's frame defect, not a new value question.

**Decisions taken here (operator overrides later):**
1. **Roster netlocs.** `hosts[]` gains three Tier C entries `{host: catalog.data.gov, tier: C, agency: data.gov, netloc_of: www.data.gov}` (and NIST, GSA likewise). The frame stays 19 hosts in the agency sense; the roster is 22 netlocs. Register `fss_scan_netlocs_2026-09 = 22` beside the existing `fss_scan_hosts_2026-09 = 19` (unchanged, its description already means agency-level hosts). Surface count stays 65. This is a new DataFile version of the targets, not an edit in place: `scan_targets_fss_2026-09` v2 with `derived_from` pointing at v1 and this task named.
2. **The unadmitted EIA flagship row** (`scan-eia-flagship-2-eia-survey-forms`, robots disallows it for this UA) is handled as cycles 1 and 2 handled unobservable surfaces: it stays on the targets, produces no per-leg Finding, is counted in `scan_surfaces_unobservable_<cycle>`, and its reason (`robots_disallowed`) is on the Observation. Refusals are findings (DD-060). No new rule.
3. **Surface ids.** `well_known` rows keep `doc_id: host:<netloc>` (continuity with two cycles of Results and A12). `home` rows get `doc_id: home:<netloc>`. `machine` rows: `machine:<netloc>`. Tier-0 legs are asked once per host, on the row that carries them today (well-known for A12 and the robots-derived legs; home for the page-derived legs), exactly as cycles 1 and 2 did; a leg asked of two rows of the same host is a defect the re-derivation gate will surface as a duplicate Finding identity. Record the scheme in DD-061 (append a clause; DD-061 is opened by the preceding task).
4. Everything else as `2026-09-08_scan_run_3.md` §1.3 onward, verbatim, with the RESULT filename changed.

**Zero edits to:** shipped rule modules, `scan_targets_fss_2026-09` v1, events, the G1 harness, `assessment/cq/*.yaml`, framework content beyond the `measured` write-back, `params.yaml` beyond `cycle.*`, any prior cc_task or RESULT.

**Immutable once written. Glob `2026-09-08_scan_run_3b_ADDENDUM*.md` before starting and again before §2.**

---

## 0. Precondition
`cc_tasks/2026-09-08_a8_v4_blind_pointer_and_fixture_table_RESULT.md` exists, gate PASS, `CURRENT` includes `RULE-A8-v4`. Otherwise stop and report.

## 1. Pre-flight (fixtures only, no federal host)
1. Targets v2 per decisions 1 and 3; `fss_scan_netlocs_2026-09` registered; the figure assertion and the Tier C rendering block from scan-run-3 §1.1 (not built there) built now.
2. `cycle.name: scan_<UTC date at this step>`; `cycle.targets: scan_targets_fss_2026-09` v2; register `params_hash`. `refuse_clobber` confirmed.
3. Six-fixture control gate against the derived tables (precondition re-check, not this task's gate). Hard stop on failure.

## 2. Run
As scan-run-3 §2, over the 65 surfaces on 22 netlocs.

## 3. Gate (the one gate of this task)
As scan-run-3 §3: byte-identical re-derivation for this cycle and all prior payloads; zero Tier C Findings outside `tier0.legs`; zero duplicate Finding identities across rows of one host; hygiene green; `git status --porcelain corpus/` shows only this cycle's promoted bodies and events.
**Failure writes no Results and no page: report and stop, RESULT with the block on top, commit, push.**

## 4. Register — as scan-run-3 §4, plus `blind_pointers` per leg where A8 applies.
## 5. Status, figures, page — as scan-run-3 §5.
## 6. Report — as scan-run-3 §6, RESULT `cc_tasks/2026-09-08_scan_run_3b_RESULT.md`. Include the request count per netloc and, separately, which of the three new netlocs answered the identified client (catalog.data.gov's CKAN API is on record as 404 to it). `seldon cc complete`; commit, push.

**SEQUENCING:** §0 → §1.1 → §1.2 → §1.3 (hard stop) → glob addenda again → §2 → §3 (hard stop) → §4 → §5 → §6 → push.
