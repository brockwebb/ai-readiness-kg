# CC Task — third re-judgement: cycles 1 to 4 under generation 9 (RULE-A3-v6, RULE-B3-v3)

**Date:** 2026-09-11
**Project:** ai-readiness-kg
**Authored by:** Desktop session, OODA on `2026-09-11_absence_claims_under_scope_limitation_RESULT.md` §4 and §8 items 1, 2.
**Fulfils:** its own ResearchTask (`seldon cc register`). Absorbs ResearchTask `016149c3` (cycle 1 re-judge) and `24af5222` (F5 global default), both closed as superseded in §4. Runs before the cycle-4 report revision and before any cycle 5.
**Spend:** zero model calls. **Network: none.** Stored Observations only. A test asserts the socket counter is zero on every new payload.

**Why this task exists.** Decision 4 of the previous task measured it: the existing re-judgements still carry absence verdicts from RULE-A3-v5 that excluded a forbidden candidate and answered anyway: cycle 2 re-judged 2, cycle 3 re-judged 1, cycle 4 re-judged 10, cycle 1 measured 1. The L0 report quotes cycle 3 re-judged and its revision will quote cycle 4. A report may not carry a verdict the auditor's rule calls an adverse opinion on an unexamined item.

**Decisions taken here (operator overrides later):**
1. **All four cycles, under CURRENT (generation 9).** Names continue each cycle's own sequence: `scan_2026-09-07_rj2`, `scan_2026-09-07b_rj3`, `scan_2026-09-09_rj2`, `scan_2026-09-10_rj2`. Cycle 1 is in this time; the previous RESULT's "never re-judged at all" is wrong (`scan_2026-09-07_rj1` is a stored payload) but it was never re-judged under harness-v5, and that is the reason it belongs here.
2. **Same procedure as `2026-09-10_rejudge_2_3_4.md`**: Observations from the stored payload, judged by CURRENT, Findings-only payload, evidence resolved through the source cycle. A test asserts that the set of legs whose verdict differs from the immediate predecessor payload is a subset of `{A3, B3}`; a third leg moving is a stop.
3. **Register only what moved**, with supersession pointers in each new Result's description, under the family-prefixed names the registrar now requires. Comparison records are written for all three families (scan, L0, figure inputs) so the figures' evidence-bound fallback is licensed for every unchanged name; no record, no fallback, hard error.
4. **Both invariant readings pin at zero on the four new payloads**, asserted, not xfailed. The eight historical pins stay as history; nothing pinned is edited.
5. **`cycle_over_cycle` gets per-cycle predecessor entries for the four new names and loses its global default** (`24af5222`): an unconfigured cycle raises. Predecessors: cycle 1 re-judged has none (the figure declines it), cycle 2 `_rj3` against cycle 1 `_rj2`, cycle 3 `_rj2` against cycle 2 `_rj3`, cycle 4 `_rj2` against cycle 3 `_rj2`. Figures and the graph page are rendered for all four to prove resolution; **the report is not rebuilt** here. The cycle-4 revision rebuilds it from `scan_2026-09-10_rj2`.

**Zero edits to:** rule modules, harness, stored payloads, prior Results, prior RESULTs, cycle evidence, targets, fixtures, `docs/reports/`, the historical invariant pins.

**Immutable once written. Glob `2026-09-11_rejudge_1_2_3_4_gen9_ADDENDUM*.md` before starting and again before §3.**

---

## 1. Decisions 1, 2. Judge the four. Stop if any leg outside `{A3, B3}` differs from its predecessor payload; the RESULT names it first.
## 2. Decisions 3, 4, 5. Register what moved; comparison records; pins; F5 predecessors and default removal; render figures and page for the four.
## 3. Gate (the one gate of this task)
Four new payloads; moved-leg set ⊆ `{A3, B3}` for each, with counts; both invariant readings 0 on all four; byte-identical re-derivation of all 16 stored payloads under their own harness and rule versions; every registered Result carries a supersession pointer and none repeats an unchanged value; comparison records present for three families; figures and graph page resolve for all four cycles with 0 `UnlicensedFallback`; F5 raises on an unconfigured cycle; socket counter 0; `make gate-task`; `make gate-full` detached, logged, polled; `seldon verify`; protected paths, `docs/reports/` untouched.
**Failure writes no Results and no payloads: report and stop, RESULT with the block on top, commit, push.**
## 4. Report
RESULT `cc_tasks/2026-09-11_rejudge_1_2_3_4_gen9_RESULT.md`: per cycle, which A3/B3 verdicts moved from what to `error`, with the blind candidate named; Results registered per family; the F5 predecessor table; every premise wrong. Close `016149c3` and `24af5222` as superseded by this task via the CLI (verify state first). `seldon cc complete`, commit, push. Final message states the per-cycle moved counts and whether the push succeeded.

**SEQUENCING:** §1 (stop on a third leg) → §2 → glob addenda → §3 (detached, logged, polled) → §4 → push.
