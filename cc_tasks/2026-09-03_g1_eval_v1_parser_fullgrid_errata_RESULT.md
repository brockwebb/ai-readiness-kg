# RESULT: G1 EVAL v1 — errata, suppression/reliability acquisition, sealed-holdout parser v1, full grid, re-score, DD-034

**Readiness gate (pre-registered, step 6): PASSED — 2 of 64 sealed-holdout records unparseable, share 0.031 ≤ 0.10** (`g1_v1_holdout_unparseable`, `g1_v1_holdout_n`; `tests/test_g1_preservation.py::test_holdout_readiness_gate` passes against `assessment/results/g1_v1_holdout.json`).

**Task:** `cc_tasks/2026-09-03_g1_eval_v1_parser_fullgrid_errata.md` (no addenda: globbed `…_ADDENDUM*.md`, none). **Sequencing honoured:** the holdout was elicited (run `g1_eval_v1_holdout_2026-09-03`) only after the parser freeze commit `0add833` (`g1-parser-v1-frozen`); `_g1_parse.py` has not changed since. **Dates:** 2026-09-03 UTC. **Model calls:** three declared runs under DD-022, each at the task's 2,000,000 ceiling, `claude -p` under Max OAuth (`claude-opus-5`, prompt epoch `g1-v0-2026-09-02` unchanged).

## 1. Errata and Result annotation (zero spend)

- **`docs/research/2026-09-02_g1_eval_prior_art_ERRATUM-01.md`** — Seldon DesignNote `21e3d2df`. The memo itself was not a Seldon artifact; it was registered (`g1_eval_prior_art_memo`, DesignNote `54dee043`) so the erratum could point at it. **Relationship used:** the ontology has no `corrects` and rejects every DesignNote→DesignNote pairing probed (`affects`, `cross_references`, `supersedes`, `informs`, `derived_from`, `related_issue`, `cites`, `context_for`, `supports`, `validates` — each names other valid from/to types). The correction is recorded as properties on the erratum (`corrects = 54dee043…`, `corrects_name = g1_eval_prior_art_memo`) and in its text. What the erratum withdraws: memo §2.3's StatCan row (bands and suppression were snippet-sourced from the §5 web-search row; the held 12-539-X 6e text is process guidance) and memo §4.5's "StatCan CV bands and suppression" constraint, until a product guide is held (step 2 landed one the same day).
- **Issue `0d314dff` "v0 parser artifacts in pilot prefix"** (factual_error, importance high, urgency low, detection audit, target data). Issue→Result is likewise unsupported (`affects`, `related_issue`, `remediated_by` all reject a Result target), so the eleven affected `g1_pilot_*` Result ids are carried in the Issue description and in an `affected_result_ids` property, with the evidence file and the misreading rule for each of the two scored artifacts (DP delta `10⁻¹⁰` → 10 via NFKC; CV direct bare "8.7%"). No Result value or state changed; no "corrected by eye" number was registered.

## 2. Acquisition (zero spend)

`scripts/g1srp_list_2026-09-03.yaml` → `harvest_triage.py` → `manifest_triage.py`, batch-027, epoch `g1srp-2026-09-03`, `docs/research/2026-09-03_g1srp_manifest_summary.json`. Corpus 212 → **216**; `kg.manifest verify` clean.

| candidate | verdict | reason |
|---|---|---|
| NCHS Data Presentation Standards for Proportions (Series 2 No. 175, 2017) | **admitted** `nchs-2017-data-presentation-standards-proportions` | the federal suppression standard: sample-size, absolute- and relative-CI-width rules with "suppressed" / "flagged for statistical review" outcomes and a worked appendix (Table I, NHANES blood pressure) |
| NCHS Data Presentation Standards for Rates and Counts (Series 2 No. 200, 2023) | **admitted** `nchs-2023-data-presentation-standards-rates-counts` | companion standard; previous-guidance section states the 20-event, RSE 23 %, 30-observation and RSE 30 % rules; Table A the current ones |
| StatCan *Guide to the Labour Force Survey 2025* (71-543-G) | **admitted** `statcan-71-543-g-guide-labour-force-survey-2025` | states Category 1/2/3 (≤ 16.5 %, ≤ 33.3 %, > 33.3 %), the minimum-size release table (7.4), approximate-CV tables, a worked unemployment-rate CV and a 68 % / 95 % CI example — the first current guide with the bands, per the task's rule |
| StatCan *Methodology of the LFS* ch. 8 (71-526-X) | staged-not-admitted (`R5_already_held`) | same construct as the guide; staged with provenance so the citation reproduces |
| Census CV-based ACS reliability statement | **cut** (`not_a_producer_rule`) | the held ACS handbook (ch. 7) says "There are no hard-and-fast rules for determining an acceptable range of error in ACS estimates"; the 12 % / 40 % thresholds in circulation are an ESRI white paper's (web search), not the Bureau's |
| ACS Data Release Rules | **admitted** `census-acs-data-suppression-rules` | the handbook's Data Suppression page is a landing page whose document is the linked PDF; the first capture was the navigation page (23 KB of chrome), re-fetched as the PDF — states the median-CV > 0.61 table filter |

No candidate was bot-blocked; none was already held under another id.

**Fixtures** (`fixture_version: v1-2026-09-03`, regenerated by anchor slicing; `tests/test_g1_fixtures.py` re-checks every passage against the corpus files and every class against the floor):

| class | dev v0 → v1 | holdout v0 → v1 |
|---|---|---|
| MOE | 8 → 8 | 5 → 5 |
| CI | 4 → 6 | 1 → 2 |
| SE | 7 → 9 | 6 → 7 |
| CV | 7 → 9 | 2 → 3 |
| RELIABILITY_FLAG | 2 → **6** | 1 → **3** |
| SUPPRESSION | 0 → **8** | 0 → **5** |
| DP_NOISE | 7 → 7 | 2 → 2 |
| VINTAGE | 11 → 13 | 4 → 5 |
| propositions | 22 → 37 | 9 → 18 |

Where a producer publishes a threshold rule rather than a flagged cell, the proposition's estimate is the published threshold and the qualifier is the producer's outcome word (recorded in the fixture header). Producer flag / suppression vocabulary from the four sources was added to the parser with the fixtures, before any elicitation, and is source-cited in the rule. One fixture correction after the dev run and before the holdout: `g1-n175-rf-001`'s polarity (the span's own outcome at width ≤ 0.05 is "can be presented"; the first dev direct response read the rule correctly) — recorded in the fixture's `notes`.

**Schedule** — `scripts/gen_g1_schedule.py` regenerates `assessment/config/g1_pilot.toml` from the fixtures: one indirect call per passage (a passage shared by both splits is elicited once) and one direct call per (proposition, qualifier class): **84 development + 35 held-out steps = 119 calls; 4,046,000 tokens at the 34,000 floor** (development 2,856,000; the holdout split's 47 calls include 12 shared-passage indirects that reuse dev evidence). Runner: `--split`, `--evidence-dir`, and evidence reuse — a slot with a response is never re-elicited (`PreservationProbe.existing_evidence`, falling back to the parent directory for the holdout).

## 3. Development grid (model spend, run 1 and run 2)

The dev grid exceeds 2.0M at the floor, so per the task's own rule it ran as two declared runs:

| run | ceiling | calls | reused | tokens settled | stop |
|---|---|---:|---:|---:|---|
| `g1_eval_v1_dev_2026-09-03` | 2,000,000 | 63 | 6 (the v0 prefix) | 1,983,468 | refused at call 70 (`over_ceiling`) |
| `g1_eval_v1_dev_2026-09-03_b` | 2,000,000 | 15 | 69 | see `kg.spend status` | schedule complete (84/84) |

The holdout file was not elicited in this step (the runner's `--split dev`; `assessment/evidence/g1/` holds 84 files, all dev).

## 4. Parser v1 (zero spend; commit `0add833`, message tag `g1-parser-v1-frozen`)

Every rule is motivated by a named dev response, cited in its docstring and reproduced verbatim in `tests/fixtures/g1/restatements.yaml` `v1_cases` (24 cases, 22 verbatim from evidence files, 2 synthetic and so marked):

1. **Pre-normalisation** (`normalise_text`, before any NFKC pass, each a named tested function): `normalise_superscripts` (`10⁻¹⁰` / `10^-10` / `10**(-10)` / `10−10` → `1e-10`), `strip_markdown_emphasis`, `expand_currency_abbreviations` (`£201m`), `flatten_pipe_tables` (header-aware; a header's "(£ millions)" scales its cells; generic headers dropped), `join_label_lists` (keyword-colon-list, blank lines allowed, nearest keyword wins), `join_derivation_lines` ("For Colorado: … ≈ 1.1 percent"), `collapse_equations` ("SE = MOE / 1.645 = 3,860 / 1.645 ≈ 2,347" → "SE ≈ 2,347"). The normalised text is stored in every record's observations.
2. **Direct-mode leading-number rule** (`_direct_leading_number`): the first unit-compatible number that is neither the estimate nor a confidence-level phrase is a candidate; `_pick` prefers a value match. Indirect mode unchanged.
3. **Level-derived SE transformations**: SE ↔ MOE (`se_from_moe`) and SE ↔ *symmetric* CI bounds (`se_from_bounds`), z from `harness.toml [g1.z_by_level]` (0.68 / 0.90 / 0.95 / 0.99) or the qualifier's own `z` (LFS: 1.0 / 2.0); plus SE restated as a ± with the SE's own value (`se_as_pm`, L3). SE now reaches L3.
4. **`parser_version`** required on `EvalResult`; `PARSER_VERSION = "g1-parse-v1"`. The v0 prefix re-scored under `g1-parse-v0` (`scripts/rescore_g1.py --stamp`) reproduces the pilot (18 / 10 / 8 / 8) — DataFile `d9c9b1fa`; under v1 — DataFile `8304ce6a`.
5. **Readiness test rewritten**: `test_dev_coverage_is_reported_but_is_not_the_gate` prints the dev-restatement coverage; `test_holdout_readiness_gate` reads the sealed-holdout results file and is skipped until one exists.
Also: formula guards (a value followed by an operator, decimal denominators, fractions inside arithmetic), "± N standard errors" as a multiple, dash ranges, a bare "CI" cue, CI candidate ranking against the qualifier's level and bounds, derived-SE candidate choice by value then binding.

## 5. Sealed holdout elicitation (model spend, run 3)

Run `g1_eval_v1_holdout_2026-09-03`, ceiling 2,000,000, `--split holdout --evidence-dir assessment/evidence/g1/holdout`: 35 new calls, 12 shared-passage indirect slots reused from the dev evidence (never re-elicited), 1,139,283 tokens settled, no refusal, schedule complete. Evidence: `assessment/evidence/g1/holdout/` (35 files, committed). A first launch crashed before declaring the run (a relative evidence path; no call, no ledger record) and was relaunched with the path resolved.

## 6. Readiness, re-score, Results

**Gate:** see the first line. Both holdout unparseables are *genuine omissions* the reviewer confirms (the response never states the qualifier), not parser misses — see the review below.

**Re-score under parser v1** (`scripts/rescore_g1.py`; each file a Seldon DataFile; every number below is a registered Result with prefix `g1_v1_<split>_…`, `computed_from` the split's reviewed DataFile and `generated_by` Script `rescore_g1` `82fdba84`; 146 dev + 147 holdout + 147 pooled + 101 prefix-v1 + 94 prefix-v0 Results):

| split | file (DataFile) | responses | records | scored | unparseable | L3+ | rate | Wilson 95 % |
|---|---|---:|---:|---:|---:|---:|---:|---|
| v0 prefix under **g1-parse-v0** (stamp) | `g1_v0rescore_reviewed` `e9edb6f1` | 6 | 18 | 10 | 8 | 8 | 0.800 | [0.490, 0.943] |
| v0 prefix under **g1-parse-v1** | `g1_v1_prefix_reviewed` `b2062326` | 6 | 18 | 18 | 0 | 17 | 0.944 | [0.742, 0.990] |
| development | `g1_v1_dev_reviewed` `f6413e7f` | 84 | 132 | 131 | 1 | 130 | 0.992 | [0.958, 0.999] |
| **sealed holdout** | `g1_v1_holdout_reviewed` `75dd17ea` | 47 | 64 | 62 | 2 | 57 | **0.919** | **[0.825, 0.965]** |
| pooled | `g1_v1_pooled_reviewed` `13710646` | 119 | 196 | 193 | 3 | 187 | 0.969 | [0.934, 0.986] |

**Holdout, per class × mode** (`g1_v1_holdout_<CLASS>_<mode>_*`):

| class | mode | n | scored | unparseable | L3+ | rate | Wilson 95 % | levels |
|---|---|---:|---:|---:|---:|---:|---|---|
| CI | direct | 2 | 2 | 0 | 1 | 0.500 | [0.095, 0.905] | L0 1 · L1 0 · L2 0 · L3 1 · L4 0 |
| CI | indirect | 2 | 2 | 0 | 1 | 0.500 | [0.095, 0.905] | L0 1 · L1 0 · L2 0 · L3 1 · L4 0 |
| CV | direct | 3 | 3 | 0 | 3 | 1.000 | [0.439, 1.000] | L0 0 · L1 0 · L2 0 · L3 0 · L4 3 |
| CV | indirect | 3 | 3 | 0 | 2 | 0.667 | [0.208, 0.939] | L0 1 · L1 0 · L2 0 · L3 0 · L4 2 |
| DP_NOISE | direct | 2 | 1 | 1 | 1 | 1.000 | [0.207, 1.000] | L0 0 · L1 0 · L2 0 · L3 0 · L4 1 |
| DP_NOISE | indirect | 2 | 2 | 0 | 2 | 1.000 | [0.342, 1.000] | L0 0 · L1 0 · L2 0 · L3 0 · L4 2 |
| MOE | direct | 5 | 5 | 0 | 5 | 1.000 | [0.566, 1.000] | L0 0 · L1 0 · L2 0 · L3 4 · L4 1 |
| MOE | indirect | 5 | 5 | 0 | 4 | 0.800 | [0.376, 0.964] | L0 1 · L1 0 · L2 0 · L3 4 · L4 0 |
| RELIABILITY_FLAG | direct | 3 | 3 | 0 | 3 | 1.000 | [0.439, 1.000] | L0 0 · L1 0 · L2 0 · L3 1 · L4 2 |
| RELIABILITY_FLAG | indirect | 3 | 3 | 0 | 3 | 1.000 | [0.439, 1.000] | L0 0 · L1 0 · L2 0 · L3 2 · L4 1 |
| SE | direct | 7 | 7 | 0 | 7 | 1.000 | [0.646, 1.000] | L0 0 · L1 0 · L2 0 · L3 0 · L4 7 |
| SE | indirect | 7 | 6 | 1 | 5 | 0.833 | [0.436, 0.970] | L0 1 · L1 0 · L2 0 · L3 1 · L4 4 |
| SUPPRESSION | direct | 5 | 5 | 0 | 5 | 1.000 | [0.566, 1.000] | L0 0 · L1 0 · L2 0 · L3 0 · L4 5 |
| SUPPRESSION | indirect | 5 | 5 | 0 | 5 | 1.000 | [0.566, 1.000] | L0 0 · L1 0 · L2 0 · L3 0 · L4 5 |
| VINTAGE | direct | 5 | 5 | 0 | 5 | 1.000 | [0.566, 1.000] | L0 0 · L1 0 · L2 0 · L3 0 · L4 5 |
| VINTAGE | indirect | 5 | 5 | 0 | 5 | 1.000 | [0.566, 1.000] | L0 0 · L1 0 · L2 0 · L3 0 · L4 5 |
| **all** | | **64** | **62** | **2** | **57** | **0.919** | [0.825, 0.965] | L0 5 · L1 0 · L2 0 · L3 14 · L4 43 |

By mode (holdout): indirect 31 scored, 27 L3+, rate 0.871 [0.711, 0.949]; direct 31 scored, 30 L3+, rate 0.968 [0.838, 0.994]. Failure classes: {'quantity_hallucination': 3, 'binding_error': 2}. Estimate status: {'wrong': 2, 'exact': 57, 'absent': 3, 'rounded': 2}.

Pooled: 196 records, 193 scored, 3 unparseable, 187 L3+, rate 0.969 [0.934, 0.986]; levels L0 6 · L1 0 · L2 0 · L3 53 · L4 134; failure classes {'quantity_hallucination': 4, 'binding_error': 2}; estimate status {'exact': 179, 'absent': 8, 'wrong': 4, 'rounded': 5}; indirect rate 0.948 [0.884, 0.978] vs direct 0.990 [0.944, 0.998].

**Genuine-loss count** (reviewer CC; criterion recorded on each reviewed file: a loss is genuine when the raw response, read in full, does not state the qualifier's class and value — or the as-of date, or the producer's outcome word — for that estimate in any form; a response that states it in a form the parser could not read is a parser miss; judgments per record in `review_note`):

| split | review queue (L0/L1/L2 or unparseable) | genuine losses | parser misses | Results |
|---|---:|---:|---:|---|
| v0 prefix under v0 | 10 | 0 | 10 | `g1_v1_prefix_v0_genuine_losses` … |
| v0 prefix under v1 | 1 | 0 | 1 | `g1_v1_prefix_v1_…` |
| development | 2 | 1 | 1 | `g1_v1_dev_genuine_losses` … |
| **holdout** | 7 | **5** | 2 | `g1_v1_holdout_genuine_losses` … |
| pooled | 9 | 6 | 3 | `g1_v1_pooled_genuine_losses` … |

The six genuine losses, by kind: four **omissions** (the $15,000–$19,999 Loudoun row's MOE and CV never restated in a summary that gave the bracket a band, "20–30 %"; the 0.83-point and 1.17-point NHANES standard errors dropped in favour of the interval; the ONS £526.8 million SE dropped) and two **D7 corruptions** (the same ONS interval restated as "£41.6 billion to £43.7 billion … give or take roughly £1.0 billion", a coarser rounding than the published precision, scored L0 by pre-registration — D7 is not loosened here; whether "rounded to the summary's own precision" deserves a level of its own is a design question for the calibration run, reported not absorbed). Three of the six carry a wrong failure-class label (`binding_error` / `quantity_hallucination` where the truth is omission) because the scorer matched another row's ± or a band phrase — v2 item.

The three parser misses: the exact "£42,649m ± £1,032.5m" outranked by a quoted bounds sentence (CI candidates pre-empt ±-form candidates; the pm-source/bounds-candidate tolerance is not symmetric with the bounds-source case); the "≤5-percentage-point / 95%-of-the-time" hyphenated coverage; and, in the dev set, an exact "MOE of 10,127" outranked by rounded bounds. All v2 (`_g1_parse.py` is frozen); listed for the next task.

**Pre-registered expectations (D8) against pooled v1 counts, with the v0-prefix-only verdicts under v1 for comparison:**

| | statement | pooled counts | verdict | v0 prefix under v1 |
|---|---|---|---|---|
| E1 | indirect loses qualifiers more than direct (Du 2026) | indirect 91/96 = 0.948 [0.884, 0.978] vs direct 96/97 = 0.990 [0.944, 0.998] | **underpowered** (direction as expected; intervals overlap) | 13/14 vs 4/4, underpowered |
| E2 | omission (L1) exceeds corruption (L0) | L1 = 0, L0 = 6 of 193 scored | **not supported** — but every L1-class loss the reviewer found was recorded by the scorer as L0 or unparseable (see above), so the scorer's L1 bucket, not the model, is what this verdict measures; the reviewer's count is 4 omissions vs 2 corruptions | underpowered |
| E3 | form_shift (L2) is the most frequent non-omission failure (van der Bles 2019) | non-omission failures: quantity_hallucination 4, binding_error 2; L2 = 0 | **not supported** — no verbal-band restatement occurred in 196 records; the model either carried the number or dropped it | underpowered |
| H1 | CV / flag / suppression lost more than MOE / CI | 67/68 = 0.985 [0.921, 0.997] vs 38/42 = 0.905 [0.779, 0.962] | **underpowered**; direction *against* the hypothesis | 5/5 vs 2/3, underpowered |
| H2 | VINTAGE omitted more than numeric qualifiers | 0/36 [0, 0.096] vs 0/113 [0, 0.033] | **underpowered**; no vintage was dropped anywhere | underpowered |

Holdout-only verdicts: E1 underpowered (27/31 vs 30/31), E2 not supported (0 vs 5), E3 not supported, H1 underpowered (21/22 vs 11/14, again against), H2 underpowered (0/10 vs 0/36).

## 7. DD-034 and mechanical updates

DD-034 appended (`docs/design_decisions.md`; DesignNote `d64f366b`): parser versioning as an instrument version; readiness on sealed holdout output with the elicit-after-freeze ordering; pre-normalisation before NFKC and why; z from the proposition's level; the erratum and what it withdraws. Skeleton v0.2.5 (G1 Evidence cell + §9 gap closed); protocol §9 one sentence on parser versioning.

## 8. Verification

| check | value |
|---|---|
| `assessment/` suite | 409 passed (was 375 at task start; 408 + the holdout gate now running) |
| root `tests/` suite | 642 passed |
| `seldon verify` | 1 issue: `docs/corpus/acquisition_candidates.md` modified — by the overnight biblio cron (T0 coverage 64/212 → projection rewrite), not this task; left for that pipeline |
| `python -m kg.spend status` | `g1_eval_v1_dev_2026-09-03` settled 1,983,468, 1 refusal (the planned stop); `…_b` settled 469,328; `g1_eval_v1_holdout_2026-09-03` settled 1,139,283; total 3,592,079 over the three declared runs, each under its 2,000,000 ceiling and under the daily band |
| `kg.manifest verify` | clean, 216 included |

## 9. Discrepancies (premise vs live state) — reported, not reconciled

1. **The dev grid exceeded 2.0M at the floor** (84 calls × 34,000 = 2,856,000): split into two declared runs, ids `g1_eval_v1_dev_2026-09-03` and `g1_eval_v1_dev_2026-09-03_b`, per the task's rule.
2. **Evidence reuse needed a code change** (the fetch/evaluate separation working as designed): `PreservationProbe.existing_evidence` and the runner's `--split` / `--evidence-dir`; the holdout run's shared-passage slots fall back to the parent directory. Reported as instructed.
3. **The registry cannot carry `corrects` or Issue→Result links** (§1): properties and descriptions carry them.
4. **A fixture correction after the dev run** (`g1-n175-rf-001` polarity, recorded in the fixture's `notes`; `fixture_version` unchanged) — the first dev direct response read the rule correctly and would have scored L0 under the mis-specified flag.
5. **Producer vocabulary was added to the parser with the fixtures at step 2**, before any elicitation (source-cited in the rule), not motivated by a dev response — the fixtures are unusable without their own outcome words. Everything else in v1 is response-motivated.
6. **The first holdout launch crashed before declaring** (relative path); no spend, relaunched.
7. **The E2 verdict is about the scorer's label, not the model**: the reviewer's reading gives 4 omissions vs 2 corruptions; the scorer's L1 bucket is empty because omitted qualifiers were caught as `binding_error`/`quantity_hallucination` on neighbouring numbers or as unparseable. A failure-class attribution rule ("no candidate bound to this estimate matches → omission, not binding error") is the first v2 item.
8. **Four working-tree files are modified by the overnight biblio cron** (`docs/corpus/acquisition_candidates.md`, `docs/corpus/operator_pickup.md`, `events/batch-024.jsonl`, `state/t2_priority.json`); not committed by this task.
9. **The schedule commit message of `9fbf61d` says 39 holdout steps**; the generated file has 35 (the message was written from an earlier count). The file is authoritative.

## Not done, by the task's boundary

Compression conditions, product-surface fixtures, a second consumer model, the surfaced leg, model-judge scoring, product-level thresholds, prompt changes, edits to the memo / v0 RESULT / any Result; parser v2 rules motivated by holdout responses (listed above for the next task).
