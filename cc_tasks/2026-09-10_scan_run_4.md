# CC Task — scan-run-4: the complete frame, first cycle under the fixed instrument

**Date:** 2026-09-10
**Project:** ai-readiness-kg
**Authored by:** Desktop session, OODA on `2026-09-10_scan_frame_v5_RESULT.md` (gate PASS; targets v5: 72 surfaces, 19 sites, 22 netlocs, 0 pending).
**Fulfils:** its own ResearchTask (`seldon cc register`). Closes `520ec74b` items (1) and (3); items (2), (4), (5) and the event-log replay fixture remain queued.
**Spend:** zero model calls. **Network:** the 22 netlocs of targets v5 and no other, robots-first, same-site (DD-062, DD-063), identified UA 0.2 (DD-060), 1 req/s per site, one worker, shared `link_probe`, one HEAD per link. Report the request count per site. Expect roughly 2,600 requests against cycle 3's 2,341.

**What is new since cycle 3, all already gated:** robots-first in the fetcher; site-key contact bound; `RULE-A5-v2` (off-site sitemap declared → `not_applicable`, observed not followed); `RULE-A8-v4` (blind pointer → `error`); seven flagship surfaces; `flagship:` prefix. `CURRENT` is whatever `params.yaml` binds today; this task changes no rule.

**Decisions taken here (operator overrides later):**
1. `params.cycle.targets` → `scan_targets_fss_2026-09_v5`; `cycle.name: scan_<UTC date at that step>`; `params_hash` registered. This is the one wiring step frame-v5 left, and it is taken here because a cycle runs here.
2. **A12 is registered per tier from the start** (`_tierA_pass/_fail/_error`, `_tierC_pass`), and pooled beside them. Cycle 3's post-hoc decomposition is the precedent; here it is done at registration.
3. **The report is not rebuilt.** `docs/reports/` is cycle 3 and unread by the operator. Cycle 4 registers Results, matrices (`scan_matrix_tierA/tierC/product_<cycle>`) and refreshes the graph page (`docs/progress/`) and figures as every cycle does. No report prose or PDF changes.
4. Refusals: BLS, BTS, SSA flagships will answer 403; the three hosts' host-level and flagship rows are recorded as refused. `error` never means the product failed (DD-052 §6). `scan_refusal_consecutive_measurements_<cycle>` continues the count.

**Zero edits to:** shipped rule modules, `params.yaml` beyond `cycle.*`, targets v5, prior Results, prior RESULTs, report prose and PDF, the G1 harness, `assessment/cq/*.yaml`.

**Immutable once written. Glob `2026-09-10_scan_run_4_ADDENDUM*.md` before starting and again before §2.**

---

## 1. Pre-flight (loopback only, no federal host)
1. Decision 1. `refuse_clobber` confirmed against every existing `state/scan_*.json`.
2. Seven-fixture control gate against derived tables under `CURRENT`, `unknown` = 0; both-clocks agreement 0 differences. **Hard stop on failure.**

## 2. Run
`run.py::main` over targets v5, the whole frame, one pass. Bodies retained per the standing retention decision. Detached, logged, polled.

## 3. Gate (the one gate of this task)
Byte-identical re-derivation for this cycle and all prior payloads (9 of 9); zero Tier C Findings outside `tier0.legs`; zero duplicate Finding identities across rows of one host; every request in the log is same-site to a roster site key and preceded by that netloc's `robots.txt` read (the replay test from DD-062, run on this cycle's log: 0 refused, 0 off-site, 0 robots-late); hygiene green; `git status --porcelain corpus/` shows only this cycle's promoted bodies and events; fast tier; `seldon verify`; protected paths.
**Failure writes no Results and no page: report and stop, RESULT with the block on top, commit, push.**

## 4. Register
Leg rates per tier, A12 per tier and pooled (decision 2), surfaces/unobservable/refusals, `scan_l0_product_legs_at_zero_<cycle>`, the three matrices, `params_hash`, `blind_pointers` per leg where A8 applies, request count per site. Upper-95 bounds per leg at the actual n (rule of three), as cycle 3 did.

## 5. Page and figures
Graph page and F1–F6 refreshed from the graph. Report untouched (decision 3).

## 6. Report
RESULT `cc_tasks/2026-09-10_scan_run_4_RESULT.md`, written after EXIT=0 logs exist: gate first; request count per site; what moved from cycle 3 and whether the instrument or the host moved it, leg by leg for the four rules that changed; the four newly live flagships, leg by leg; the three refused flagships; anything in the first cycle of a surface that says the declaration was a poor one (item 3 of frame-v5's RESULT §6); every premise this task got wrong. `seldon cc complete`, commit, push. Final message states whether the RESULT exists and the push succeeded.

**SEQUENCING:** §1.1 → §1.2 (hard stop) → glob addenda again → §2 → §3 (hard stop) → §4 → §5 → §6 → push.
