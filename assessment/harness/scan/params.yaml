# Every constant the scan harness uses. NOTHING numeric is hard-coded in a collector — a test
# greps `collectors/` for integer literals outside an allowlist and fails on a new one.
#
# Why (task 2026-09-06_harness_scaffold.md §2.3, prior art Khan 2026 / Wintermute
# wm-20260906-075432-d860df): one unswept truncation constant was worth 14 points. A constant
# that lives in a collector cannot be swept, cannot be versioned, and cannot be stamped on the
# evidence it shaped. `params_hash` rides on every Observation, so a Finding always names the
# parameter set that produced its evidence.
params_version: "0.1.0"

# How a Finding's id is derived — a PARAMETER, not code, because it is an identifier scheme
# and an identifier scheme is versioned data (`cc_tasks/2026-09-07_scan_harness_v3.md` §0).
#
#   1  `rule_version` is stamped from the constant `rules/_common.RULE_VERSION`, which is the
#      literal "v1" for every rule ever shipped. Every Finding on the log to 2026-09-07 was
#      made this way, and the constant is an INPUT to the derived `finding_id`
#      (`model.py::Finding.make`), so correcting it in place would re-identify all 1,353.
#   2  `rule_version` is stamped from the rule id itself (`rules.parse_rule_id`), which is the
#      only place the version is actually true. `RULE-A3-v4` stamps `v4`.
#
# History re-derives under ITS OWN params (the gate recovers them from git), so raising this
# moves no stored id: a cycle measured under identity 1 is re-judged under identity 1. This is
# the same move the event log makes with `schema_version` on every line — the scheme is data
# that travels with the record, never an edit to the past.
finding_identity: 2

# How `rules/_common.unobserved_error` names the reason a probe was not observed — a PARAMETER
# for the same reason `finding_identity` is one: a Finding's reason string is inside the
# byte-identical re-derivation comparison, so a corrected sentence is versioned data and never
# an edit to the past (`cc_tasks/2026-09-13_rule_a12_v3.md` decision 2).
#
#   1  the error CLASS's note, printed whichever branch of `_common.unobserved` fired. Every
#      Finding on the log to 2026-09-13 was made this way, and three of them say "the collector
#      observed the surface" about a probe the same sentence calls unobserved — because the
#      class was `None` and the REFUSAL STATUS is what fired.
#   2  the note for the branch that fired: a blind class names the class, a status in
#      `manners.unobservable_statuses` names the status and says what the list says about it.
#
# History re-derives under ITS OWN params (the gate recovers them from git by hash), so raising
# this moves no stored reason: a cycle measured under scheme 1 is re-judged under scheme 1.
reason_text: 2

# TIER 0 — the headline legs, and the only legs a Tier C reference host is judged on.
#
# `cc_tasks/2026-09-08_scan_frame_fss.md` names this set and `..._RESULT.md` §9.2 records that
# it did not exist yet; `cc_tasks/2026-09-08_scan_run_3.md` §1.1 declares it BEFORE the cycle
# that uses it, which is the whole point — a headline set chosen after the numbers are in is a
# choice of numbers.
#
# What makes a leg tier 0: it asks about a HOST THAT PUBLISHES DATA rather than about a
# statistical product. robots.txt served, discovery files present, a deep link and a bogus
# route, the declared machine layer, whether the declared and enforced layers agree, and
# whether error measures are present as structured fields. A federal data publisher outside the
# statistical system is the same kind of thing as one inside it at that level, which is what
# licenses the Tier C comparison (DD-059) — and above it, a catalog has no product vintage and
# no bulk download, so the comparison would stop being like-for-like.
tier0:
  legs: ["A4", "A5", "A10", "A11-declared", "A12"]
  # A12 is a CANDIDATE indicator (DD-054): reported beside the others, counted in no fraction.
  # It is in the tier-0 set because declare-vs-enforce is a host property and the single most
  # informative one for a reference host; it is not in any denominator.
  candidate_legs_in_set: ["A12"]

  # Legs WITHDRAWN from the host-level instrument (DD-066). Not deleted and not re-judged:
  # every G1-D Observation and Finding in cycles 1 to 4 and the self cycle stays on the log
  # under its own params_hash and harness_version (DD-052 §3, DD-064 §2). What stops is the
  # measuring.
  #
  # The construct is right and the LEVEL was wrong. G1-D asks whether an estimate carries,
  # beside it and machine-readably, the fields that make it interpretable — margin of error,
  # standard error, CV, suppression flag, reliability marker. A host's home page carries no
  # estimate, so `fail` there is structural rather than a measurement of the agency: 126 fail
  # and 28 error across every body and every reference host in every cycle, and not one pass
  # (`cc_tasks/2026-09-15_g1d_leaves_l0_RESULT.md` §0). On PRODUCT surfaces the same rule
  # passes 104 times, which is the same evidence read the other way round: the property is
  # carryable, and the home page is not where it is carried.
  #
  # Read by `run.targets` for `home` surfaces and by `build_l0_matrices` for the published
  # matrices' metadata. The leg keeps its rule module and keeps being dispatched on PRODUCT
  # surfaces (`flagship`), where the January G1 EVAL pilot is the instrument of record.
  legs_withdrawn:
    - leg: "G1-D"
      from_surfaces: ["home", "well_known"]
      effective: "cycle 5 (this params_hash forward)"
      decision: "DD-066"
      construct: >-
        uncertainty present for the human reader and absent from the markup
      why: >-
        the host-level surface is a home page, which carries no estimate, so the leg cannot
        hold the property it measures and every verdict on it is structural

#: **The judgement layer's own version** (`cc_tasks/2026-09-10_harness_v5_blind.md` decision 2).
#: Rules are versioned per leg and a Finding records the rule that made it, but the SUBSTRATE
#: those rules share — what `errors.py` calls blind, what `_common.unobserved` answers — was
#: not versioned at all, so changing it would have re-scored every stored payload under a
#: reading that did not exist when they were measured.
#:
#: A params set that binds no version IS a v4 set (`errors.HARNESS_DEFAULT`), and `rederive`
#: recovers each payload's own params from git by hash. That is the whole mechanism by which
#: nine stored payloads keep re-deriving byte-identically while the layer underneath them
#: changes: no payload is edited and no version is inferred.
#:
#: v5: `robots_disallowed` is BLIND — forbidden to look at a URL inside the product is not the
#: same as a URL outside it. Cycle 4 shipped the consequence of the old reading (a `pass` from
#: two probes that were never issued) and its gate stopped the run.
harness_version: 5

cycle:
  # Which cycle these parameters belong to, and which target list it measures. Named here so
  # a Finding can be traced to its cycle without a reader having to know which state file was
  # current on the day, and so `params_hash` moves when the target list does — a measurement
  # of a different set of surfaces is a different measurement.
  # Cycle 2. `b`, not a new date: the second cycle ran on the SAME UTC day as the first, and
  # the name is what every output path, every Result suffix and `refuse_clobber` key on — so
  # a colliding name would point cycle 2 at cycle 1's payload. DD-041's rerun convention is
  # exactly this suffix (`kg_diag_<metric>_2026-09-04b` against the un-suffixed first run),
  # and DD-056 takes the suffix from here, so this cycle's Results read `..._2026-09-07b`.
  # Cycle 3, the first over the whole frame (16 + 3 agencies, 22 netlocs).
  # Cycle 4 (`cc_tasks/2026-09-10_scan_run_4.md` decision 1), the first over the COMPLETE
  # frame: targets v5 adds the seven operator-declared flagships, so no body is measured
  # without a product surface. New UTC date, so a new name and no suffix; `refuse_clobber`
  # keys on it and `state/scan_2026-09-10.json` does not exist. The targets key is the one
  # wiring step `2026-09-10_scan_frame_v5.md` deliberately left: changing it changes what a
  # cycle measures, and that task ran no cycle.
  name: scan_2026-09-10
  targets: scan_targets_fss_2026-09_v5

manners:
  # RFC 9309: the scanner obeys the file it measures.
  # ONE identity, with a contact URL a host operator can actually reach (RFC 9309 practice).
  # DD-060: the instrument never varies its identity to get a better answer — retrying as a
  # browser would produce data, and the data would measure our willingness to misrepresent
  # ourselves rather than the product's accessibility. 0.2 from the 2026-09-08 frame task.
  user_agent: "ai-readiness-kg-scanner/0.2 (+https://github.com/brockwebb/ai-readiness-kg)"
  requests_per_second_per_host: 1.0
  connect_timeout_seconds: 10.0
  read_timeout_seconds: 30.0
  max_retries: 3
  backoff_base_seconds: 2.0
  backoff_on_status: [429, 503]
  follow_redirects: true
  max_redirects: 5
  # Paths that are always fetched because they ARE the object of measurement — a robots.txt
  # that disallows /robots.txt cannot hide it from a measurement of robots.txt.
  always_fetch_paths: ["/robots.txt", "/sitemap.xml", "/sitemap_index.xml", "/llms.txt",
                       "/data.json", "/.well-known/"]
  # No byte cap by default. §2.1: whole body retained, no truncation anywhere. A cap must be
  # set explicitly to exist, and setting one is a decision that shows up in params_hash.
  max_body_bytes: null
  # The contact bound is the SITE, and a site is a ROSTER HOST with one leading `www.`
  # stripped (DD-063, superseding DD-062's registrable-domain definition). A netloc belongs to
  # the site if it equals the key or ends with `.` + the key, which is RFC 6265 §5.1.3
  # domain-matching, adopted rather than invented.
  #
  # DD-062 used the Public Suffix List and it answered a different question: `www.ers.usda.gov`
  # resolved to `usda.gov`, `nces.ed.gov` to `ed.gov`, so 22 netlocs became 17 sites, three
  # separately recognized agencies became one contact unit, and whole department domains came
  # into scope. The registrable domain is right for cookie scope and wrong for "which host is
  # this agency". No suffix list is consulted and no such dependency remains.
  site_bound:
    definition: "roster host with one leading www. stripped"
    match: "equal to the key, or ending in '.' + the key (RFC 6265 §5.1.3)"
    resolver: null
  # Statuses at which we did not OBSERVE the surface, we were REFUSED it. A 404 is a real
  # measurement (the path is not served); a 403/401/429 is the host declining this client, and
  # scoring it `fail` publishes an absence claim about a product nobody was allowed to look at.
  # www.bls.gov answered 403 to all 60 requests in the first smoke run and both BLS surfaces
  # scored `fail` on all 15 legs; that is what this list exists to stop. `error_class` cannot
  # carry the distinction because `http_4xx` covers both.
  unobservable_statuses: [401, 403, 407, 429]

crawl:
  # A3 bulk-access link discovery: one hop from the product page, per §3's signal.
  depth: 1
  max_links_followed: 40

# ---------------------------------------------------------------------------------------
# The FRAME: who is measured, and how a surface is chosen for them.
# `cc_tasks/2026-09-08_scan_frame_fss.md` §1 and §2. Declared BEFORE any agency page was
# fetched, which is the whole point: a selection rule written after looking at the listings is
# a preference wearing a rule's clothes (`targets.yaml` makes the same argument for cycle 1).
# ---------------------------------------------------------------------------------------
frame:
  # WHERE THE ROSTER COMES FROM. Two documents, both retained content-addressed under
  # corpus/evidence/frame/ and both cited by the roster DataFile. Neither is typed into a
  # script: `build_roster.py` parses the bodies.
  #
  # They disagree, and the reconciliation rule is declared here rather than decided after the
  # counts were seen (see `roster_authority`).
  roster_sources:
    - name: icsp_charter
      url: "https://statspolicy.gov/assets/files/ICSP%20Charter.pdf"
      role: membership
      note: >-
        The ICSP's own constitutive document (April 2023). Enumerates every member with an
        explicit legend — `^` a Recognized Statistical Agency or Unit, `*` a designated
        Statistical Official, `**` the Chair — so both tiers are a PARSE of a flag, not a
        reading of prose.
    - name: statspolicy_about
      url: "https://statspolicy.gov/about/"
      role: current_urls_and_crosscheck
      note: >-
        The live rendering of the same fact, and the only source that carries an agency HOME
        URL per recognized agency. Used for URLs and as a cross-check on the counts.
  # Which source settles MEMBERSHIP when the two disagree, and why — stated as a rule about
  # the kind of document each is, so it cannot be chosen after the fact to get a number.
  #
  # Recognition under CIPSEA 2018 (44 U.S.C. 3561(11), 3562) is a legal status OMB confers.
  # The charter is the Council's constitutive document and enumerates its membership under a
  # flag legend; the About page is a rendered summary of the same fact and is authoritative
  # for what it alone carries — the current home URL of each agency. So: membership from the
  # charter, URLs from the About page, and every disagreement recorded on the roster's face
  # rather than resolved silently.
  roster_authority: icsp_charter
  # The legend both sources use. Read from the documents, restated here only so the parser has
  # a declared vocabulary rather than an inline literal.
  flags:
    recognized_agency_or_unit: "^"
    statistical_official: "*"
    chair: "**"
  # A member line the parser must not mistake for an entry: the legend line itself begins with
  # the flag character it defines.
  legend_line_tokens: ["denotes a recognized", "denotes a statistical", "denotes the chair"]

  # SURFACE SELECTION, per agency. §2, and every clause is a token list rather than a
  # judgement so the rule can be run, tested and re-run.
  machine_entry_point:
    tokens: ["api", "developers", "developer", "data-access", "open-data", "datasets"]
    # Nothing matched is a RECORDED OBSERVATION about the agency, never a gap in the roster
    # and never a hand-picked substitute.
    none_found_marker: no_machine_entry_point
  flagship_products:
    # The first N product links in the AGENCY'S OWN ORDER on its own listing page. Order is
    # the agency's editorial judgement and is the nearest thing to a published "principal
    # products" list that every agency has; taking the first N is a rule, choosing among them
    # is a preference.
    n: 3
    listing_tokens: ["data", "statistics", "publications", "products", "surveys", "tables"]
    # A link is a PRODUCT only if it goes deeper than the listing page itself and is not one
    # of these. Same discriminators as cycle 1's `targets.yaml`, reused rather than re-derived.
    reject_tokens: ["about", "contact", "help", "faq", "news", "blog", "careers", "privacy",
                    "accessibility", "foia", "policy", "policies", "glossary", "sitemap",
                    "subscribe", "newsletter", "search", "home", "espanol", "español"]
    min_anchor_words: 2
    none_found_marker: no_flagship_products
  # THE ENTERPRISE DATA INVENTORY, and why §2's first rule was thrown away.
  # `..._ADDENDUM-02.md`. The replaced rule scraped anchor SUBSTRINGS off an agency home page:
  # it found fewer real surfaces than the hand list it was meant to replace (12 flagships for
  # 16 agencies against cycle 1's 26 for 13), and it found some by accident — `api` inside
  # "Capital Markets" on the Federal Reserve's staff page. A rule that finds surfaces by
  # accident is not a sampling statement.
  #
  # The replacement is prior art rather than scraping. Every CFO Act department must publish a
  # machine-readable Enterprise Data Inventory at `/data.json` under the OPEN Government Data
  # Act (44 U.S.C. 3511, continuing OMB M-13-13), in DCAT-US: the department's OWN declaration
  # of its datasets, with `publisher` and `bureauCode` per dataset, a `landingPage`, and typed
  # `distribution` entries that say which are APIs. Uniform across the frame, machine-parseable,
  # and already the object of a framework indicator.
  edi:
    path: "/data.json"
    # WHICH HOSTS ARE PROBED for an inventory. Derived from the roster by rule, because the
    # department -> domain mapping is on NEITHER named roster source: the ICSP charter and the
    # About page both name departments in prose and neither carries a URL for one. So the rule
    # probes the agency's own host and its registrable domain, records which served, and
    # records a null where neither does. Where a department's domain is not the agency's
    # registrable domain (census.gov sits under Commerce, bls.gov under Labor) the outcome is a
    # RECORDED null and never an invented host; resolving those properly needs the
    # organization list at catalog.data.gov, which is named for the next task.
    probe_agency_host: true
    probe_registrable_domain: true
    # A distribution is an API when its declared format/mediaType says so, or when its
    # accessURL sits on an api/developer path. Word-boundary matched, never substring.
    api_format_tokens: ["api", "rest", "graphql", "odata"]
    api_path_tokens: ["api", "apis", "developer", "developers", "data-access"]
    # How many datasets the operator shortlist offers per agency, most recently modified first.
    # A SHORTLIST, never a selection: the inventory carries no field that ranks datasets, so
    # ranking them would be a value judgment the source does not make.
    shortlist_n: 5
    # An agency with fewer declared flagships than this gets a shortlist and is marked
    # `flagships_pending_operator_declaration`.
    min_declared_flagships: 2

  # DEPARTMENT -> INVENTORY URL, the two sources `..._ADDENDUM-03.md` names.
  # Both are declared here rather than typed into a script, and both turned out to contain
  # something other than what the addendum expected — which is why the script MEASURES them
  # instead of working around them. See `scripts/resolve_department_domains.py`.
  dept_domains:
    # CISA's .gov registry. Authoritative for WHICH DEPARTMENT OWNS A DOMAIN (reverse), and
    # silent on which domain is a department's primary one (forward): one row per domain, no
    # primary-domain field, 28 rows for Labor and 124 for HHS.
    dotgov_registry_url: "https://raw.githubusercontent.com/cisagov/dotgov-data/main/current-federal.csv"
    dotgov_source: "CISA .gov registry, cisagov/dotgov-data, current-federal.csv"
    # What the addendum expected to find, kept so the mismatch is on the record rather than in
    # a commit message.
    dotgov_columns_expected: ["Domain name", "Agency", "Organization"]
    # data.gov's harvest source registry — the authoritative record of where the government
    # itself reads each department's data.json from. Every documented endpoint is probed, and
    # the STATUS of each is the measurement: ADDENDUM-01 declared Tier C because a federal
    # catalog that is not machine-legible is a finding in its own right.
    catalog_api_urls:
      - "https://catalog.data.gov/api/3/action/organization_list?all_fields=true"
      - "https://catalog.data.gov/api/3/action/harvest_source_list"
      - "https://catalog.data.gov/api/3/action/package_search?rows=0"
    datajson_harvest_type: "datajson"

  # TIER C — reference hosts, NOT statistical agencies. `..._ADDENDUM-01.md`.
  #
  # Declared, not parsed: they are not on the ICSP source because they are not part of the
  # statistical system, and that is the point. A tier-0 check — robots.txt served, discovery
  # files present, deep link and bogus route, declared machine layer, declare-vs-enforce
  # coherence — is a property of a HOST THAT PUBLISHES DATA, not of a statistical product, and
  # at that level a federal data publisher outside the statistical system is the same kind of
  # thing as one inside it. If the government's own catalog is not machine-legible at tier 0,
  # that is a finding in its own right.
  #
  # ABOVE tier 0 the comparison stops being like-for-like — error-measure fields, product
  # vintages, bulk download of a product are claims about a statistical product and a catalog
  # has none — so Tier C appears on NO leg outside `tier0.legs`, in no Tier A or Tier B
  # denominator, and on no agencies x legs matrix. `tests/test_scan_figures.py` asserts it.
  #
  # StatCan stays OUT: the tier-0 comparison holds for federal hosts under ONE legal regime,
  # which is what makes it a comparison rather than decoration.
  tier_c_source: "operator declaration 2026-09-08 (cc_tasks/2026-09-08_scan_frame_fss_ADDENDUM-01.md)"
  tier_c:
    - name: data.gov
      home: "https://www.data.gov"
      machine_entry_point: "https://catalog.data.gov"
      reason: "the federal DCAT catalog, GSA-operated; catalog.data.gov is its machine entry point"
    - name: NIST
      home: "https://www.nist.gov"
      machine_entry_point: "https://data.nist.gov"
      reason: "NIST's data portal is its declared machine entry point"
    - name: GSA
      home: "https://www.gsa.gov"
      machine_entry_point: "https://open.gsa.gov"
      reason: "GSA's developer/API landing"

  # Tier B is measured at the host layer only: there is no product listing to read, because a
  # department without a recognized statistical unit publishes no official statistics as such.
  tier_b_surfaces: ["home", "robots.txt"]

probes:
  # ONE same-host policy, for EVERY collector that dereferences a URL it discovered on a
  # surface. `cc_tasks/2026-09-08_scan_harness_v4.md` §1.3.
  #
  # It used to be `link_probe.same_host_only`, which is where `collectors/links.probe` reads
  # it — and `collectors/v2clauses.follow_latest_pointer` read nothing at all, so A8
  # dereferenced `public.govdelivery.com` on the Census ACS surface after its anchor matcher
  # took "subscribe to govdelivery email updates" for a latest-vintage pointer
  # (`cc_tasks/2026-09-07_scan_run_2_RESULT.md` §6.1). Two requests, robots obeyed, no verdict
  # resting on it — and the `link_probe` comment named the exposure exactly ("probing off-host
  # would make the control cycle reach the real internet before the gate that exists to run
  # before any real host") while governing only one of the two collectors.
  #
  # A policy enforced in one of the two places it applies is not a policy. Both collectors now
  # go through `manners.on_roster_host`, which reads THIS key; `link_probe.same_host_only` is
  # kept readable for the cycles measured under it and `manners.same_host_only` REFUSES if the
  # two ever disagree — two keys that can differ are two policies.
  same_host_only: true

a1_formats:
  # A1: which Content-Types / extensions count as structured data rather than PDF-only.
  structured_content_types: ["text/csv", "application/json", "application/geo+json",
                             "application/vnd.apache.parquet", "application/x-parquet",
                             "application/vnd.ms-excel",
                             "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
                             "application/xml", "text/xml"]
  structured_extensions: [".csv", ".json", ".geojson", ".parquet", ".xlsx", ".xls", ".xml", ".tsv"]
  pdf_content_types: ["application/pdf"]

a4_crawlers:
  # The AI-crawler user agents A4 and A11-declared evaluate robots.txt against. A list, in
  # params, because it changes: GPTBot did not exist in 2022 and something else will not exist
  # in 2027.
  user_agents: ["GPTBot", "ClaudeBot", "Claude-Web", "PerplexityBot", "Google-Extended",
                "CCBot", "Bingbot", "Applebot-Extended"]

a5_discovery:
  well_known_probes: ["/llms.txt", "/.well-known/ai-plugin.json", "/.well-known/mcp.json"]
  # How many sitemap URLs are kept on the Observation for a reader's convenience. NOT
  # evidence truncation — the whole body is stored content-addressed either way — but it is
  # still a constant, and the lint was right to refuse it inside the collector.
  sample_urls_retained: 5

a6_markup:
  # extruct syntaxes to extract, and the types that satisfy A6.
  syntaxes: ["json-ld", "microdata", "rdfa"]
  dataset_types: ["Dataset", "DataCatalog", "http://schema.org/Dataset",
                  "https://schema.org/Dataset"]
  dcat_classes: ["http://www.w3.org/ns/dcat#Dataset", "http://www.w3.org/ns/dcat#Catalog"]

a8_freshness:
  header_fields: ["last-modified", "date"]
  markup_fields: ["dateModified", "datePublished", "temporalCoverage"]

a9_m2m:
  probes: ["/openapi.json", "/swagger.json", "/api/openapi.json", "/llms.txt",
           "/.well-known/mcp.json"]

a10_soft404:
  # A soft-404 is HTTP 200 on a route that should not exist.
  invalid_path_suffix: "/__ai-readiness-kg-probe-404__"
  # Rendering half needs a browser; see collectors/lighthouse.py for what happens without one.
  renderer: "none"

d1_licence:
  markup_fields: ["license", "licence", "http://schema.org/license",
                  "http://purl.org/dc/terms/license"]
  recognised_prefixes: ["https://creativecommons.org/", "http://creativecommons.org/",
                        "https://spdx.org/licenses/", "https://opensource.org/licenses/",
                        "https://www.usa.gov/government-works", "https://opendefinition.org/"]
  recognised_tokens: ["CC0", "CC-BY", "PUBLIC DOMAIN", "U.S. GOVERNMENT WORK", "MIT",
                      "APACHE-2.0", "ODC-BY"]

d4_catalog:
  paths: ["/data.json"]
  # Project Open Data v1.1 required fields on a dataset entry.
  required_dataset_fields: ["title", "description", "keyword", "modified", "publisher",
                            "contactPoint", "identifier", "accessLevel"]

f4_changelog:
  paths: ["/changelog", "/changelog.json", "/releases", "/whatsnew", "/news"]
  machine_readable_content_types: ["application/json", "application/atom+xml",
                                   "application/rss+xml"]

g1d_uncertainty:
  # G1-D: error measures present as STRUCTURED FIELDS beside the estimates, not footnotes.
  # Column-header tokens that name an error measure.
  field_tokens: ["margin of error", "moe", "standard error", "se", "cv",
                 "coefficient of variation", "confidence interval", "ci", "lower bound",
                 "upper bound", "±"]

e5_control:
  # The control cycle is the instrument's own E5. A cycle with zero fired controls is INVALID.
  #
  # PRE-REGISTERED (`cc_tasks/2026-09-07_scan_harness_v3.md` §1.4): every entry below was
  # DERIVED FROM THE RULE SOURCE and written down BEFORE the fixtures were first run. A gate
  # whose expectation is edited to match what came out is not a gate, so a mismatch is a
  # finding to report, never a line to change.
  #
  # A value may be a single verdict (every leg expects it) or a table with a `default` and
  # per-leg exceptions. Two fixtures need the table: the interesting branches are exactly the
  # legs that behave DIFFERENTLY from the rest, and a blanket expectation would hide them.
  #
  # **The four DCAT-US field legs (B1, B4, D3, G4) take every fixture's DEFAULT, and no table
  # gains a row** (`cc_tasks/2026-09-18_dcat_field_rules.md`). Derived from the rule source
  # before the fixtures were run with them, as every entry below was. Each reads D4's catalog
  # observation and nothing else, so on every fixture it sees exactly what D4 sees and lands
  # where D4 lands: `passes_all` serves a catalog whose product record carries every field
  # (`pass`); `fails_all` serves no `/data.json`, a 404 is observed (`fail`, no catalog record);
  # `refuses_identified_client` and `resets_connection` blind the catalog (`error`, via
  # `_common.only_errors`); the other five serve `passes_all`'s `/data.json` unblinded (`pass`).
  #
  # **Generation 12 (`cc_tasks/2026-09-18_schema_field_rules.md`): B1 (now `RULE-B1-v2`), B2 and
  # D2.** Derived from the rule source before the fixtures were run with them. `passes_all`'s
  # page now carries a `variableMeasured` whose `measurementTechnique` is a complete
  # `DefinedTerm`, and its robots.txt — and the three fixtures that serve their own robots.txt
  # with `passes_all`'s content (`refuses_identified_client`, `robots_forbids_product`,
  # `sitemap_on_sibling`) — a `Content-Signal` declaring `ai-train` and `ai-input`. So:
  #   B1, B2  take every fixture's default. B1-v2 passes where either half passes and errs where
  #           an unpassed half is blind: `refuses_identified_client` blinds the page and the
  #           catalog (`error`), `resets_connection` everything (`error`), `fails_all` serves no
  #           catalog and a page with no markup (`fail`); the rest serve `passes_all`'s page and
  #           catalog unblinded (`pass`). B2 reads only the page, which the same fixtures blind or
  #           strip the same way.
  #   D2      reads the DECLARED layer, like A4, so it departs from the default exactly where A4
  #           does: `refuses_identified_client` serves its robots.txt (200) with the signal
  #           (`pass`), and `robots_404_html` serves none (`fail`, "no robots.txt served").
  #           `fails_all` answers /robots.txt with an HTML shell, which the collector reads as
  #           absent (`fail`, the default).
  #   B5      is judged per BODY after a cycle's surfaces (`run.judge_bodies`), never per
  #           surface, and each fixture is served on its own port — so no control fixture is a
  #           body with two products and B5 appears in no row here. Its fixtures are in
  #           `tests/test_schema_field_rules.py`.
  expected_verdicts:
    passes_all: "pass"
    fails_all: "fail"
    # Declared layer permits, enforced layer refuses — A12's central branch, and what
    # www.bls.gov did to the first smoke run. Derivations:
    #   default `error`  every content leg sees HTTP 403, which `errors.classify_status` calls
    #                    `refused` and `errors.CLASSES` marks blind, so `_common.only_errors`
    #                    is true and each rule returns `error` rather than scoring an absence
    #                    claim about a product nobody was allowed to look at.
    #   A4   `pass`      A4 judges the DECLARED layer alone; robots.txt is served (200) and
    #                    allows the product path for every AI-crawler UA in `a4_crawlers`.
    #   A11-declared `pass`  its two declared-layer sources are robots.txt (served, permits)
    #                    and the page's meta-robots (the 403 body carries none). The robots
    #                    observation is a real 200, so `only_errors` is FALSE and the rule
    #                    reaches its verdict. This is the entry the task file's blanket
    #                    "every content leg error" would have got wrong.
    #   A12  `fail`      robots permits this UA for the probe path and the host answers 403 to
    #                    that same path: `rule_a12.py` branch 5, the declared and enforced
    #                    layers disagree.
    # A product whose robots.txt PERMITS the page and FORBIDS three of the paths its legs
    # probe, plus A10's invented invalid route.
    # `cc_tasks/2026-09-11_control_fixture_robots_forbids_product.md` decisions 1 and 2.
    #
    # DERIVED, not transcribed from a run. The rule is decision 2's: a leg whose DECISIVE probe
    # the partition forbids expects `error`, because harness-v5 (DD-064) says a URL inside the
    # product that robots forbids is BLIND and no verdict about the product may rest on one.
    # Every other leg expects what it expects on `passes_all`, because this fixture serves the
    # same bytes.
    #
    #   A10 `error`  its invalid-route probe is forbidden. Under harness-v4 this fixture
    #                returns `pass` from "invalid route correctly HTTP None" — the cycle-4
    #                incident, replayed at the control layer where it was missing.
    #   A8  `error`  the latest-vintage pointer it resolves is forbidden.
    #   A3  `error`  the link anchored "whole-product archive" is forbidden, and a `fail` means
    #                "no whole-product download", which that evidence cannot establish.
    #   B3  `error`  /methodology.html is forbidden, and a `fail` means "no methodology document
    #                reachable", which that evidence cannot establish either.
    #   A1  `pass`   NOT error: three of its links are forbidden and four are served, and one of
    #                the served ones IS structured data. A `pass` rests on something seen. The
    #                asymmetry is the point — see the RESULT.
    robots_forbids_product:
      default: "pass"
      A10: "error"
      A8: "error"
      A3: "error"
      B3: "error"
    # Everything `passes_all` serves, except /robots.txt, which answers 404 with an HTML error
    # page. `cc_tasks/2026-09-13_rule_a12_v3.md` decision 1.
    #
    # DERIVED FROM THE RULE SOURCE and written here BEFORE the fixture was ever run, as every
    # entry above was. The fixture blinds NOTHING — a 404 is an observation, `http_4xx` is
    # OBSERVED in `errors.CLASSES`, and the Fetcher's robots gate reads an unparseable body as
    # permitting everything — so `fixture_expectations` derives no row from dispatch and every
    # entry here is a rule's judgement over served content:
    #
    #   default `pass`  the surface is `passes_all` byte for byte, and only the three legs that
    #                   READ robots.txt can tell the difference.
    #   A4   `fail`     `collectors/robots.py` sets `present: false` (status >= 400), so A4-v1
    #                   takes its "no robots.txt served" branch: absence permits retrieval and
    #                   declares nothing.
    #   A11-declared `fail`  its declared layer is robots.txt plus the page's meta-robots; the
    #                   first is absent and `passes_all`'s index carries no meta-robots, so
    #                   nothing is declared.
    #   A12  `fail`     `present` is false and 404 is not in `manners.unobservable_statuses`,
    #                   so both versions reach the "nothing is DECLARED" branch and both say
    #                   `fail`. **The verdict is the same under v2 and v3 and the REASON is
    #                   not**, which is the whole point of this fixture: v2 says the file was
    #                   served with the wrong content type, v3 says it was not served.
    robots_404_html:
      default: "pass"
      A4: "fail"
      A11-declared: "fail"
      A12: "fail"
      D2: "fail"
    refuses_identified_client:
      default: "error"
      A4: "pass"
      A11-declared: "pass"
      A12: "fail"
      D2: "pass"
    # The socket is reset before any response, robots.txt included. Every collector raises,
    # `errors.classify_exception` returns `connection_reset`, which is blind — so every leg is
    # `error`, with no exceptions. A12 lands on its "the robots/probe pair was not collected"
    # branch rather than on a coherence verdict, which is the right answer: a host that cannot
    # be reached has not been shown to be incoherent.
    resets_connection: "error"
    # PRE-REGISTERED before the fixture was first run (`cc_tasks/2026-09-08_scan_harness_v4.md`
    # §1.2), derived from the rule source. The fixture serves everything `passes_all` serves
    # and resets the connection on A10's invented invalid route ALONE, so:
    #   default `pass`  every leg but A10 sees exactly what it sees on `passes_all`; not one
    #                   of them fetches `a10_soft404.invalid_path_suffix`.
    #   A10  `error`    the valid-route probe is served HTTP 200, so `_common.only_errors` is
    #                   FALSE and `RULE-A10-v2` would reach its status branches and return
    #                   `pass` on "invalid route correctly HTTP None" — the false positive of
    #                   `cc_tasks/2026-09-07_scan_run_2_RESULT.md` §6.2. `RULE-A10-v3` calls
    #                   `_common.unobserved_error` on the invalid-route probe first, sees
    #                   `connection_reset` (blind), and returns `error`.
    # This is the fixture that must FIRE: a control that cannot reproduce the defect cannot
    # certify the fix.
    invalid_route_unobserved:
      default: "pass"
      A10: "error"
    # PRE-REGISTERED before the fixture was first run (`cc_tasks/2026-09-08_scan_run_3.md`
    # §1.2), derived from the rule source. Every GET is served and every HEAD is reset, so:
    #   default `pass`  no other leg probes with HEAD; each sees exactly `passes_all`.
    #   A1, A3 `error`  their evidence is the shared `link_probe` leg, which HEADs every link.
    #                   All links blind and the page served is the exact state
    #                   `RULE-A1-v3`/`RULE-A3-v4` returned `fail` for; `RULE-A1-v4` and
    #                   `RULE-A3-v5` return `error` — unmeasured, not absent.
    # DERIVED, not written. `assessment/harness/scan/fixture_expectations.py` reads which
    # collectors each leg dispatches to (following `rules.consumes` for the shared link probe),
    # which HTTP method each issues, and which methods this fixture blinds. Three legs
    # dereference with HEAD — A1 and A3 through `links.probe`, A8 through
    # `v2clauses.follow_latest_pointer` — so all three are `error` here.
    #
    # A8 was pre-registered `pass` by reading RULE source and asking which legs the fixture
    # would touch. That method missed `follow_latest_pointer` entirely and the control gate
    # caught it (`cc_tasks/2026-09-08_scan_run_3_RESULT.md` §1). The row below is the
    # derivation's, and the derivation is checked against this table by
    # `tests/test_a8_v4_and_fixture_tables.py`: a difference stops, and neither side is edited
    # to match the other (DD-061).
    resets_links_only:
      default: "pass"
      A1: "error"
      A3: "error"
      A8: "error"
    # A host whose robots.txt declares its sitemap on a SECOND NETLOC OF THE SAME SITE.
    # `cc_tasks/2026-09-09_closeout_and_manners.md` §1. It serves everything `passes_all`
    # serves; the only difference is where the `Sitemap:` line points. Nothing is blinded, so
    # every row here is a content judgement and the derivation defers all of them.
    #
    # `pass` throughout, and the reasoning is the pre-registration: a same-site sibling is
    # INSIDE the contact bound (decision 2), so the declaration is followed, and the primary
    # still serves its own sitemap covering its own product, so A5 passes on that. What the
    # fixture proves is not a verdict but an ORDER: the sibling receives its robots.txt before
    # anything else. A verdict table cannot express an order, which is why the request-order
    # assertion lives in the tests and this table records only that nothing else moved.
    sitemap_on_sibling:
      default: "pass"

fuji:
  # NOT WIRED. The PyPI name `fuji` is a DIFFERENT package (startechsheffield, "a small
  # library made to make common tasks easier") — a name collision that installs cleanly and
  # imports, which is exactly how a wrong dependency gets into a harness unnoticed. The real
  # F-UJI (pangaea-data-publisher/fuji, Devaraju & Huber 2021) is a SERVER installed from
  # GitHub source with its own config and metadata catalogues; `fuji-server` is not on PyPI.
  # Standing it up is a dependency this scaffold does not take on. The three overlapping legs
  # use their own collectors and the cross-check is deferred, with the metric ids kept here so
  # the next task does not have to re-derive them.
  available: false
  deferred_metrics:
    A2: "FsF-A1-03D"
    A6: ["FsF-F4-01M", "FsF-I1-01M"]
    D1: "FsF-R1.1-01M"
  install_note: "pip install git+https://github.com/pangaea-data-publisher/fuji ; needs config/server.ini"

# ---------------------------------------------------------------------------------------
# v2 clause parameters. Task `cc_tasks/2026-09-06_scan_targets.md` §2: the 2026-09-06 rule
# review found that 12 of 16 `v1` rules implement only part of their MeasurementSpec's signal.
# Every constant a v2 clause needs lives here, under the same rule as every other constant —
# a threshold inside a rule cannot be swept, versioned, or stamped on the evidence it shaped.
# ---------------------------------------------------------------------------------------

link_probe:
  # A1-v2 and A3-v2 need the per-link Content-Type their specs ask for ("for each, HEAD and
  # read Content-Type"). v1 classified on the href suffix alone, so an extensionless endpoint
  # serving text/csv was invisible. Bounded, because a product page can carry hundreds of
  # links and this is one request each at the standing rate.
  max_links_probed: 25
  method: HEAD
  # SUPERSEDED by `probes.same_host_only`, and kept readable rather than deleted: it is the
  # key the 2026-09-07 and 2026-09-07b cycles were measured under, and their params sets are
  # recovered from git by hash for the re-derivation gate. `manners.same_host_only` reads
  # `probes.same_host_only` and refuses if this key disagrees with it — one policy, two
  # spellings, never two answers.
  #
  # Same-host only. Two reasons, and the second is the one that bites: A1 and A3 ask what the
  # PRODUCT offers, not what it links to elsewhere; and the control fixture links out to a
  # licence URL, so probing off-host would make the control cycle reach the real internet
  # before the gate that exists to run before any real host.
  same_host_only: true
  # A HEAD that a host answers 405 is retried as a ranged GET: some servers refuse HEAD
  # outright, and treating that as "no content type" would read a server quirk as a product
  # property.
  fallback_get_on_status: [405, 501]
  # ONE probe per object per cycle (`cc_tasks/2026-09-07_scan_harness_v3.md` §0, §1.3). A1 and
  # A3 ask different questions of the SAME 25 links and each HEADed all of them: 559 of the
  # 1,118 link probes in the 2026-09-07 cycle were the second request for an object already
  # fetched, and they were the largest single share of its 46-minute wall time. RFC 9309
  # governs what may be fetched and licenses nothing about fetching it twice.
  #
  # The observations are collected once under `shared_leg` and the rules that read them
  # declare `CONSUMES` (see `rules/__init__.py`). `legs_served` are the legs whose own
  # collection is therefore empty — everything they need is on the shared leg.
  shared_leg: link_probe
  legs_served: ["A1", "A3"]

a3_bulk:
  # A3-v2: "classify links as bulk (whole-product archive or full dataset file) vs filtered
  # query". Three mechanical discriminators, none of them a judgement call:
  archive_extensions: [".zip", ".gz", ".tgz", ".tar", ".tar.gz", ".7z", ".bz2"]
  archive_content_types: ["application/zip", "application/gzip", "application/x-gzip",
                          "application/x-tar", "application/x-7z-compressed"]
  # A query string is the signature of a filtered export: /export?state=CA&year=2020 is a
  # subset by construction, whatever it is served as.
  query_string_is_filtered: true
  # A whole-product file is large. Below this, a "download" is a preview or a row sample.
  # 50 KiB: the smallest whole-product CSV among the 17 admitted g1sfc surfaces is 63 KiB
  # (statcan-14-10-0287-01 LFS provinces), and the Census QuickFacts preview export is 4 KiB.
  min_bulk_bytes: 51200
  # A Content-Length a host does not send is not evidence of smallness.
  missing_length_is_bulk: false

a2_api:
  # A2-v2: "record auth scheme, declared rate limits, and whether the description is
  # machine-readable". The first two were never read.
  auth_headers: ["www-authenticate", "proxy-authenticate"]
  rate_limit_headers: ["ratelimit", "ratelimit-limit", "ratelimit-policy",
                       "x-ratelimit-limit", "x-rate-limit-limit", "retry-after"]
  # OpenAPI 3 puts the auth model at components.securitySchemes and Swagger 2 at
  # securityDefinitions; an API that documents neither has not declared an auth scheme.
  openapi_auth_keys: ["securitySchemes", "securityDefinitions", "security"]
  openapi_rate_limit_keys: ["x-ratelimit", "x-rate-limit", "rateLimit", "x-throttling"]

a6_shacl:
  # A6-v2: the spec says validate against DCAT-AP SHACL shapes. See the header of the shapes
  # file for why this is a stated MINIMAL subset rather than the DCAT-AP distribution.
  shapes_path: "shapes/dcat_ap_min.ttl"
  shapes_profile: "dcat_ap_min"
  # A graph that pyshacl cannot even load is a parse_error, never a `fail`: we did not
  # observe the product's conformance, we failed to read it.
  treat_load_failure_as: parse_error

a8_latest:
  # A8-v2: "test whether a latest-vintage pointer resolves". v1 read the date and never
  # dereferenced anything.
  link_rels: ["latest-version", "last", "canonical"]
  href_tokens: ["/latest", "latest.", "current.", "/current", "most-recent"]
  anchor_tokens: ["latest", "most recent", "current release", "current version"]
  # Anchor text that names a SUBSCRIPTION or a notification, not a vintage. A8-v3 drops a
  # candidate whose anchor text carries any of these, whatever else it matched.
  #
  # The test case is the one that fired: on
  # `scan-census-flagship-2-american-community-survey-acs` an anchor whose RETAINED text reads
  # "subscribe subscribe to govdelivery email" matched `anchor_tokens` and resolved HTTP 200 on
  # `public.govdelivery.com`, so A8 would have credited an email signup page as a resolving
  # latest-vintage pointer. It did not manufacture a verdict — A8 was 23 fail / 3 error, zero
  # pass — which is luck, not design (`cc_tasks/2026-09-07_scan_run_2_RESULT.md` §6.1).
  #
  # WHICH token matched is not recoverable from that record: `reporting.anchor_text_retained_chars`
  # is 40, the stored `how` string stops there, and the matcher scanned the whole anchor. So
  # `find_latest_pointers` now records `matched_token` beside the text — a matcher that cannot
  # say why it matched leaves a Finding nobody can check.
  #
  # An exclusion list rather than a tighter inclusion list: the inclusion tokens are what the
  # indicator MEANS ("latest", "most recent") and narrowing them would lose real pointers, while
  # what went wrong is a distinct vocabulary — a mailing list — that no vintage pointer uses.
  excluded_anchor_tokens: ["subscribe", "subscription", "sign up", "signup", "e-mail",
                           "email updates", "govdelivery", "newsletter", "notify me",
                           "get updates", "alerts", "rss feed"]
  max_pointers_followed: 3
  resolves_statuses: [200, 203, 206]

a11_meta:
  # A11-declared-v2: the signal names robots.txt AND meta-robots; only the first was read.
  meta_names: ["robots", "googlebot", "gptbot", "claudebot"]
  # A directive that withholds the product from an AI client. `noai`/`noimageai` are the
  # de-facto opt-out tokens; they are not in RFC 9309 and are recorded as convention.
  restricting_tokens: ["noindex", "nofollow", "noarchive", "nosnippet", "noai", "noimageai"]

b3_without_js:
  # B3-v2: "and whether it is retrievable without JS" — never implemented in v1.
  #
  # Prior art already in this repo: DD-030's extent gate, from Kohlschütter, Fankhauser &
  # Nejdl, *Boilerplate Detection using Shallow Text Features* (WSDM 2010). The same two
  # shallow features answer "is there a document here before JavaScript runs" as answer "did
  # this conversion produce a navigation surface". Reused deliberately rather than invented:
  # the floors below are the ingest gate's own, so a methodology page that would not have been
  # admitted to the corpus cannot be scored as legible here.
  min_visible_chars: 2000
  max_link_density: 0.25

a10_shell:
  # A10-v2: v1 decided on status codes alone and never tested for the "error shell" the
  # signal names, and never used the `pre_js_chars` its own collector records.
  error_shell_tokens: ["page not found", "not found", "sorry", "we can't find",
                       "cannot be found", "error 404", "page unavailable"]
  # A deep link whose raw HTML carries less than this is a client-rendered shell: there is
  # no product on the page until JavaScript runs. Same floor as b3_without_js, same source.
  min_pre_js_visible_chars: 2000

d1_sources:
  # D1-v2: the signal names three licence sources and v1 read one.
  # RFC 8288 Web Linking: `Link: <...>; rel="license"`.
  link_header_rels: ["license", "licence", "terms-of-service"]
  terms_paths: ["/terms", "/terms-of-service", "/policies/terms", "/about/policies",
                "/license", "/licence", "/legal"]
  max_terms_probed: 3

d4_validation:
  schema_path: "shapes/pod_v1_1_min.schema.json"
  schema_profile: "pod_v1_1_min"
  # A catalog too large to validate whole is validated on a bounded sample of its datasets;
  # the Finding records how many were checked, so nobody reads a sample as a census.
  max_datasets_validated: 200

dcat_fields:
  # The four DCAT-US structured-field legs (`cc_tasks/2026-09-18_dcat_field_rules.md`). They
  # collect NOTHING of their own: each rule declares `CONSUMES = ("D4",)` and reads the catalog
  # D4 already fetches, so a host is asked for its `/data.json` once per surface, not five
  # times. `runner.collect_leg` returns [] for these legs, as it does for `link_probe`'s.
  legs_served: ["B1", "B4", "D3", "G4"]
  # What the D4 observation is ENRICHED with (`v2clauses.dcat_record_fields`): for the product's
  # own records — D4's membership test, `product_url` appearing in the dataset entry — which
  # of these fields each record carries. A field is carried when its value is non-empty and,
  # where a `pattern` is declared, every element matches it. A dotted name is a field of the
  # record's `distribution` entries, carried when any distribution carries it.
  #
  # Every field is named by a document on disk:
  #   bureauCode, programCode, publisher, describedBy, distribution.describedBy
  #       corpus/kernel/dcat-us-1-1-schema.md (doc_id `dcat-us-1-1-schema`); the two patterns
  #       are the formats it states: "in the format of `015:11`" and "Use the format of
  #       `015:001`".
  #   hasQualityMeasurement, versionNotes, previousVersion, hasCurrentVersion,
  #   wasGeneratedBy, qualifiedAttribution
  #       corpus/kernel/dcat-us-3-dataset-schema.md (doc_id `dcat-us-3-dataset-schema`).
  #   prov:wasGeneratedBy, prov:wasDerivedFrom
  #       corpus/kernel/w3c-dcat-3.md (doc_id `w3c-dcat-3`), over the PROV-O ontology
  #       (corpus/crosswalk/w3c-prov-o-ontology.html). `wasDerivedFrom` unprefixed is the same
  #       property in a catalog whose JSON-LD context maps it.
  fields:
    bureauCode: {pattern: "^[0-9]{3}:[0-9]{2}$"}
    programCode: {pattern: "^[0-9]{3}:[0-9]{3}$"}
    publisher: {}
    describedBy: {}
    distribution.describedBy: {}
    hasQualityMeasurement: {}
    versionNotes: {}
    previousVersion: {}
    hasCurrentVersion: {}
    wasGeneratedBy: {}
    "prov:wasGeneratedBy": {}
    wasDerivedFrom: {}
    "prov:wasDerivedFrom": {}
    qualifiedAttribution: {}
  # Each rule's clauses over those fields. `any_of`: a record satisfies the clause when it
  # carries at least one of the fields; `all_of`: when it carries every one. A rule passes when
  # EVERY catalog record for the product satisfies EVERY clause — a consumer that lands on any
  # one of the product's records must find the field there. The clauses a definition names and
  # no admitted document gives a field for are declared on the rule module as `UNMEASURED` and
  # printed on every verdict, never silently dropped.
  clauses:
    B1:
      data_dictionary: {any_of: ["describedBy", "distribution.describedBy"]}
    B4:
      quality_measurement: {any_of: ["hasQualityMeasurement"]}
      revision_metadata: {any_of: ["versionNotes", "previousVersion", "hasCurrentVersion"]}
    D3:
      lineage: {any_of: ["wasGeneratedBy", "prov:wasGeneratedBy", "wasDerivedFrom",
                         "prov:wasDerivedFrom"]}
    G4:
      authority_codes: {all_of: ["bureauCode", "programCode"]}

schema_terms:
  # The schema.org structured-field legs (`cc_tasks/2026-09-18_schema_field_rules.md`). Like the
  # DCAT-US legs they collect NOTHING of their own: each rule declares `CONSUMES = ("A6",)` and
  # reads the markup A6 already extracts (`parsed.raw`), so a product page is fetched once per
  # surface. `runner.collect_leg` returns [] for these legs. B1 is served the same way and is
  # already listed under `dcat_fields.legs_served`; its v2 consumes both D4 and A6.
  legs_served: ["B2", "B5"]

b5_consistency:
  # B5 is judged once per BODY (`rules.body_groups`, decision 5), over the A6 observations of
  # the body's PRODUCT surfaces on one cycle. A surface whose id starts with one of these
  # prefixes is not a product: the host's home page and its well-known row.
  exclude_surface_prefixes: ["home:", "host:"]

content_signal:
  # D2 (`cc_tasks/2026-09-18_schema_field_rules.md` decision 6). Reads the `Content-Signal`
  # directives `v2clauses.content_signals` parses from the robots.txt A4 fetches; collects
  # nothing of its own (`CONSUMES = ("A4",)`).
  legs_served: ["D2"]
  # All three from corpus/kernel/cloudflare-content-signals-policy.md (doc_id
  # `cloudflare-content-signals-policy`): the directive, carried as "Content-Signal:
  # ai-train=no, search=yes, ai-input=no"; the three categories it defines; and the two values,
  # "either allowing (yes) or disallowing (no)". A category or value outside these is
  # `unknown_category`.
  directive: "Content-Signal"
  categories: ["ai-input", "ai-train", "search"]
  values: ["no", "yes"]
  # The two uses the indicator names — "model training/retrieval use" — in the policy's words:
  # `ai-train` "Training or fine-tuning AI models", `ai-input` "retrieval augmented generation,
  # grounding, …". Both must be declared: the policy's clause (c) says an undeclared use is
  # neither granted nor restricted, which is not addressed.
  required: ["ai-train", "ai-input"]

f4_entries:
  # F4-v2: "and carries a revision class per entry" — v1 stopped at the content type.
  revision_class_fields: ["type", "change_type", "changeType", "revision", "revision_class",
                          "category", "kind", "class", "severity"]
  # Atom/RSS carry the class as <category term="..."> on an entry.
  atom_category_elements: ["category"]
  min_entries_classified_fraction: 0.5

e5_ordering:
  # E5-v2: the signal's first sentence — "Both control fixtures are scanned before any real
  # host" — was never implemented, because a pure rule cannot read a clock. It can read
  # TIMESTAMPS it is given: the cycle passes the earliest surface observation's `captured_at`
  # alongside the control observations, and the rule asserts the controls all precede it.
  # That makes the ordering falsifiable from stored evidence rather than asserted by the
  # runner's control flow.
  require_controls_before_surfaces: true

reporting:
  # How much of a validator's output is retained ON the Observation for a reader. NOT evidence
  # truncation — the whole response body is stored content-addressed either way — but the
  # integer-literal lint was right to refuse these inside a collector: a cap nobody can sweep
  # is a cap nobody can question, which is the whole of Khan 2026's finding.
  validator_messages_retained: 10
  anchor_text_retained_chars: 40
  # Rounding precision for the ratios reported on an Observation. A ratio is a convenience for
  # a reader; every rule compares against the params floor, never against the rounded value.
  ratio_decimal_places: 4

